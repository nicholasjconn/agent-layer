# Copy to .env and fill in secrets. Do NOT commit .env.
GITHUB_PERSONAL_ACCESS_TOKEN=
CONTEXT7_API_KEY=
TAVILY_API_KEY=

# Optional: explicit parent root for ./al when no root flags are provided.
# Path must contain a .agent-layer entry that resolves to this agent-layer.
# Relative paths resolve from the agent-layer root.
PARENT_ROOT=
