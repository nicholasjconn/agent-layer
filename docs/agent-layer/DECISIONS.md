# Decisions

Note: This is an agent-layer memory file. It is primarily for agent use.

## Purpose
A rolling log of important, non-obvious decisions that materially affect future work (constraints, deferrals, irreversible tradeoffs). Only record decisions that future developers/agents would not learn just by reading the code. Do not log routine choices or standard best-practice decisions; if it is obvious from the code, leave it out.

## Format
- Keep entries brief and durable (avoid restating obvious defaults).
- Keep the oldest decisions near the top and add new entries at the bottom.
- Insert entries under `<!-- ENTRIES START -->`.
- Line 1 starts with `- Decision YYYY-MM-DD <id>:` and a short title.
- Lines 2–4 are indented by **4 spaces** and use `Key: Value`.
- Keep **exactly one blank line** between entries.
- If a decision is superseded, replace the old entry with the new one. Fold the old entry's tradeoff context into the new entry's `Reason` field when it is still valuable, then remove the old entry.
- Periodically consolidate: remove entries that are now self-evident from the codebase (the decision is embodied in code, tests, or docs and a reader would learn it without the log). When removing, verify the tradeoff information is not uniquely preserved in the log.

### Entry template
```text
- Decision YYYY-MM-DD short-slug: Short title
    Decision: <what was chosen>
    Reason: <why it was chosen>
    Tradeoffs: <what is gained and what is lost>
```

## Decision Log

<!-- ENTRIES START -->

- Decision 2026-08-04 skill-import-lock-identity: Every imported skill has its own canonical merge base and convergence state
    Decision: `.agent-layer/skills.lock.json` records source and ref evidence, commit, and canonical upstream hash independently per skill. A `[[skills.imports]]` block is configuration shorthand only: it has no locked commit, representative entry, mutable identity, or convergence state. Existing entries reconcile from their own commits; newly introduced or revealed membership locks immediately at the operation's current resolved target; an unchanged pin stays at its own commit unless explicitly retargeted or reset.
    Reason: Partial success legitimately leaves members from one block at different commits, so choosing one member by name as the block's merge base makes unrelated skills depend on arbitrary ordering. Per-skill evidence is already sufficient for pull, push, offline status, retarget detection, and selector replacement without adding a block ID or second mutable state source.
    Tradeoffs: Entries from one block may remain at different commits across runs. Lock entries duplicate resolved configuration evidence as an observed snapshot, and a manual selector replacement can remap an entry only when exactly one current block still selects its recorded path; ambiguous remote-dependent ownership fails instead of being guessed.

- Decision 2026-08-04 skill-push-heterogeneous-group-base: A missing configured branch starts from the destination default
    Decision: When `al skills push` must create a missing configured contribution branch, it starts from the destination repository's current default-branch commit. If no default branch exists, the group fails without creating a ref. A candidate absent from that base is added as its complete local tree. On a branch that already existed, an absent path is a destination-side removal and goes through ordinary three-way reconciliation.
    Reason: The destination owns the contribution branch, so its default is the only non-arbitrary base. Selecting one member's locked source made heterogeneous groups depend on configuration order. A newly created branch has no common skill history for paths absent from the destination base; merging those paths against an empty tree would misread unchanged source files as destination deletions.
    Tradeoffs: A new contribution branch always retains destination-default content and can group unrelated sources coherently, but creation is unavailable for an empty or malformed destination without a resolvable default branch.

- Decision 2026-08-05 skill-projection-exclusive-roots: Client skill roots are byte-exact disposable outputs replaced wholesale
    Decision: `.agents/skills/` and `.claude/skills/` are exclusively owned projections. Under the project lock, each canonical source is read once into an immutable `skilltree.Tree`; sync builds a complete client root at a fixed sibling staging path, moves any live root to a discard path, publishes the stage, and removes the discard. Retry deletes leftover stage/discard paths and rebuilds from canonical sources. Projection has no marker, ownership manifest, backup restoration, or journal. The imported-source transaction journal remains responsible only for canonical imported trees, configuration, and lock state.
    Reason: Client roots are derived state, so forward convergence from the two canonical source tiers is simpler and safer than tracking or restoring edits to disposable output. Reusing one tree preserves exact manifest bytes, resources, and executable bits across validation, hashing, and every client.
    Tradeoffs: A failed swap can leave a client root absent until the next sync, but never exposes a partially built root. Direct edits and independently installed content in either owned client root are unconditionally removed.

- Decision 2026-07-28 dispatch-mcp-interface: Agent Dispatch is exposed as a built-in MCP server with a long bounded wait
    Decision: Ship `dispatch_options`, `dispatch_start`, `dispatch_wait`, `dispatch_continue`, `dispatch_cancel`, `dispatch_inspect`, and `dispatch_output` on a reserved built-in `agent-layer` stdio server (`al dispatch mcp-server`), projected automatically into every enabled Codex, Claude, Antigravity, VS Code, Copilot CLI, and Grok client. `dispatch_wait` blocks for `dispatch.mcp_wait_timeout_minutes` (default 30) and every handler is bounded by `dispatch.mcp_tool_timeout_minutes` (default 40); Codex and Grok additionally receive that bound as `tool_timeout_sec`. The CLI keeps the same lifecycle operations plus inspect/output, and its eight-minute wait.
    Reason: A long MCP request lets a coordinator wait inside one tool call instead of spending turns polling `al dispatch wait`, without adding a second lifecycle store, a workflow supervisor, or MCP-side state. The 10-minute gap between the soft and hard deadlines gives recovery headroom without letting a broken tool call linger for an hour. Claude documents only a client-wide `MCP_TOOL_TIMEOUT`, so Agent Layer leaves it alone and relies on the server-side guard rather than changing every unrelated MCP server.
    Tradeoffs: Every enabled caller now carries seven extra tool schemas in always-present context. `dispatch_start`, `dispatch_continue`, and `dispatch_cancel` are annotated destructive because dispatched agents can modify their environment or cancellation can terminate provider work. An MCP `dispatch_start` is an RPC acknowledgement, so a transport disconnect after the backend starts can leave a durable run whose handle the caller never saw; inspect/output recover evidence when the invocation ID is known. Antigravity receives the same canonical definition, but agy 1.1.8 still reports no MCP runtime discovery, so its live support remains unproven.

- Decision 2026-01-24 a1b2c3d: Ignore unexpected working tree changes
    Decision: Agents will not pause, warn, or stop due to unexpected working tree changes (unstaged or staged files not created by the agent).
    Reason: The user works in parallel with agents, making concurrent changes a normal operating condition.
    Tradeoffs: Increases risk of edit conflicts if both user and agent modify the same file simultaneously; relies on git for resolution.

- Decision 2026-01-25 7e2c9f4: Agent-only workflow artifacts live in `.agent-layer/tmp`
    Decision: Workflow artifacts are written to `.agent-layer/tmp` using a unique per-invocation filename: `.agent-layer/tmp/<workflow>.<run-id>.<type>.md` with `run-id = YYYYMMDD-HHMMSS-<short-rand>`; no path overrides.
    Reason: Keeps artifacts invisible to humans while avoiding collisions for concurrent agents without relying on env vars or per-chat IDs.
    Tradeoffs: Files can accumulate until manually cleaned; agents must echo paths in chat to retain context.

- Decision 2026-02-10 p1c-init-upgrade-ownership: Init scaffolding only; user-owned config/env; agent-owned `.gitignore`
    Decision: `al init` is one-time scaffolding (errors if `.agent-layer/` exists); upgrades/repairs go through `al upgrade plan` + `al upgrade`. `.env` and `config.toml` are user-owned and never overwritten. `.agent-layer/.gitignore` is agent-owned, always overwritten, and excluded from upgrade diffs.
    Reason: Avoid clobbering user config; reduce upgrade-plan noise; simplify init semantics.
    Tradeoffs: Changes to `.agent-layer/.gitignore` cannot be preserved; repos without baseline evidence need an `al upgrade` to establish it.

- Decision 2026-02-12 chlog-immutable: CHANGELOG entries are historical and immutable
    Decision: Never modify published CHANGELOG entries. They record what happened at the time of release and are treated as fixed historical records, even if terminology or paths have since changed.
    Reason: Changing historical entries undermines trust in the changelog as a factual record and can confuse readers comparing old entries against old tags.
    Tradeoffs: Stale references in old entries (e.g., renamed files) remain; readers must consult current docs for the latest names.

- Decision 2026-02-17 p12-yolo-mode: Approvals policy is a 5-mode system including `yolo` (supersedes f6a7b8c)
    Decision: `approvals.mode = "yolo"` auto-approves commands + MCP (like `all`) and sends full-auto flags to supporting clients: Claude `--dangerously-skip-permissions`, Codex `approval_policy=never` + `sandbox_mode=danger-full-access`, Copilot CLI `--yolo`, Antigravity `--dangerously-skip-permissions`, Grok `--permission-mode bypassPermissions --always-approve`. A one-line `[yolo]` stderr acknowledgement runs on every sync/launch.
    Reason: Sandboxed/ephemeral environments want to skip all prompts without per-client setup.
    Tradeoffs: YOLO bypasses all safety prompts only where clients expose the control. No `al doctor` warning — YOLO is a deliberate choice, not a health issue.

- Decision 2026-02-18 config-resilience: Strict runtime parse + lenient repair path (supersedes config-migrate, unknown-key-repairable)
    Decision: `Config.Migrate()` is removed. Runtime commands parse strictly (`ParseConfig`/`LoadConfig`, reject unknown keys, wrapped as `ErrConfigValidation`); repair tools (`al wizard`, `al doctor`) use `ParseConfigLenient`/`LoadConfigLenient`. `al upgrade` uses `config_set_default` ops that prompt interactively for new required fields.
    Reason: Silent defaults violate the "no silent fallbacks" rule; repair tools must always be runnable; explicit consent is a design principle.
    Tradeoffs: Users must run `al wizard` or `al upgrade` to fix broken configs instead of auto-repair. Exception: configs whose only fault is a legacy key that needs a migration (validation wraps `ErrConfigNeedsUpgrade`) are not wizard-fixable — the wizard's patch preserves unknown sections verbatim, so it detects this class and redirects to `al upgrade` rather than dead-ending at sync.

- Decision 2026-02-22 claude-config-dir: Opt-in repo-local `CLAUDE_CONFIG_DIR` via `local_config_dir` config field
    Decision: Gate `CLAUDE_CONFIG_DIR=<repo>/.claude-config` behind `[agents.claude] local_config_dir = true` (opt-in, default false). Use `.claude-config/` (not `.claude/`) to avoid collision with the project-level `.claude/settings.json` generated by `al sync`; keep `.claude-config/` gitignored unconditionally.
    Reason: Claude Code writes a user-level `settings.json` to `CLAUDE_CONFIG_DIR` that would collide with Agent Layer's project-level file; UX cost (two `.claude*` directories) makes always-on inappropriate.
    Tradeoffs: Disabled by default means no isolation unless configured. Claude Code stores auth in the OS credential store regardless of `CLAUDE_CONFIG_DIR` (upstream limitation), so `local_config_dir` currently isolates settings and caches but not auth.

- Decision 2026-02-24 quiet-supersedes-p7a: Warnings verbosity policy (`reduce`, `quiet`, and `--quiet`)
    Decision: `warnings.noise_mode` supports `reduce` and `quiet`, and `--quiet`/`-q` provides command-line suppression. `reduce` suppresses only non-critical suppressible warnings. Configured `quiet` suppresses warnings, update checks, and dispatch banners for normal runs, while `al doctor` still prints warnings by default; an explicit `al --quiet doctor` suppresses warning-only doctor output while preserving failure output.
    Reason: Users need one coherent verbosity model that serves both safer daily use (`reduce`) and zero-noise scripted flows (`quiet`/`--quiet`).
    Tradeoffs: Quiet mode can hide high-risk warnings by design, and older pinned binaries may ignore quiet behavior until upgraded.

- Decision 2026-03-02 instruction-files: Primacy ordering, dedup, and user-managed `04_conventions.md`
    Decision: Instruction files are `00_rules.md`, `01_base.md`, `02_memory.md` (primacy first), and a user-managed `04_conventions.md` that is never overwritten by `al upgrade`. (Superseded in part by [cli-skill-catalog, 2026-05-28]: bare `al init` no longer seeds any instruction files including `04_conventions.md`; `04_conventions.md` is created only when the wizard installs missing workflow-bundle files.) Project-specific items (frontend rules, coverage thresholds, package policies, typing, schema safety) live in `04_conventions.md`; universal items stay in the managed instruction set. A new `append_to_file` migration kind delivers new managed-side defaults to existing repos.
    Reason: Project-specific conventions add noise where they do not apply; primacy ordering improves model compliance with hard constraints.
    Tradeoffs: Conventions are not auto-updated during upgrades; new defaults require explicit `append_to_file` entries. File renames required a v0.9.1 migration.

- Decision 2026-03-21 native-skill-sync: Native skill directory sync replaces MCP prompt delivery (supersedes 2026-01-17 e5f6a7b, 2026-02-23 mcp-prompts-dispatch-bypass)
    Decision: Removed MCP prompt delivery; `al mcp-prompts` remains only as a hidden deprecation no-op stub. Claude reads `.claude/skills/`; Codex/Antigravity/VS Code/Copilot/Copilot-CLI share `.agents/skills/`. Subdirectory support (`scripts/`, `references/`, `assets/`) is preserved.
    Reason: MCP prompts return flat text only; current client docs support directory-format Agent Skills with `.agents/skills/` as the interoperable path.
    Tradeoffs: Lost the unified MCP prompt API; gained full Agent Skills resource support and fewer duplicate projections.

- Decision 2026-05-07 mcp-catalog-seed-split: MCP server catalog moves to wizard-only embedded file; install seed ships zero `[[mcp.servers]]`
    Decision: Default MCP blocks now live in a wizard-only embedded catalog; the install seed ships only an empty `[mcp]` section with docs guidance. Interactive wizard selection inserts a selected catalog block only when it is absent from config.toml, and disables unselected defaults in place (`enabled = false`, block kept) — see wizard-mcp-disable-in-place 2026-05-29. Profile mode remains verbatim.
    Reason: Fresh `al init` should produce a minimal config instead of ~70 disabled MCP lines, while the wizard remains the place to discover curated defaults.
    Tradeoffs: Profile-mode users keep full responsibility for whatever their profile contains.

- Decision 2026-05-29 reasoning-effort-capability-matrix: Reasoning effort is per-client; model/effort compatibility is delegated to the downstream client
    Decision: Support Codex and Claude `reasoning_effort` as custom-tolerant fields (unknown values pass through with a sync warning, not a hard error); reject Copilot CLI effort until a verified control exists. For Claude, do not gate on the model — pass all values via `--effort` and write settings `effortLevel` only for non-`max` values; Claude Code is the authority on which model/effort combinations apply.
    Reason: Silent omission breaks fail-loud behavior, but strict Claude catalogs broke when upstream added values such as `xhigh`, and the prior Opus-only gate rejected valid combinations (e.g. Sonnet now supports effort) and blocked an unset model; `max` is session-only while low/medium/high/xhigh are cataloged.
    Tradeoffs: Typos surface as sync warnings instead of hard errors for Claude/Codex; effort set against a model that does not support it is applied silently by Claude Code rather than rejected by Agent Layer; Copilot configs still fail fast until support is intentionally added.

- Decision 2026-05-09 claude-antigravity-agent-specific-deep-merge: Claude/Antigravity agent_specific deep-merge; install seed denies AskUserQuestion
    Decision: Claude and Antigravity `agent_specific` are deep-merged into their generated `settings.json` files for object values (arrays/scalars still replace). `permissions.deny` is additive and silent; `permissions.allow` warns on override, and Claude also warns for `effortLevel`. Install seed ships `agent_specific.permissions.deny = ["AskUserQuestion"]` to disable Claude Code's clarification tool by default. Codex `agent_specific` remains shallow.
    Reason: Shallow-replace forced users to repeat the entire `permissions` block (re-listing managed allow entries) to add a deny. Agents in this repo prefer text-only clarifications.
    Tradeoffs: Claude and Antigravity warning helpers intentionally differ only where their managed settings differ. `cloneAgentSpecificValue` only deep-clones the projected types (`map[string]any`, `[]any`, `[]string`); new slice types need explicit support.

- Decision 2026-05-16 skill-architecture: Diff cleanup and accepted-finding fixes stay workflow-owned
    Decision: (a) Diff-scoped cleanup workflows (`prune-uncommitted-tests` and `simplify-uncommitted-code`) are references under `clean-and-fix-code`, not standalone root skills; `simplify-codebase` remains the codebase-wide sibling. (b) Re-grading workflows use reviewer rubrics that prevent the implementer's narrative from becoming the proof of completion; `verify-work` keeps a plan/request-anchored comparison rubric at `references/contract-verification-rubric.md` while remaining a hermetic root skill that does not call another skill. (c) Verbatim reviewer prompts or rubrics live as files owned by the workflow that applies them. (d) Review verdict classification lives in `review-uncommitted-code/references/finding-verdict-classification.md`; bounded accepted-finding fixes are ordinary orchestrator/agent behavior in workflows that apply them, not a standalone root skill.
    Reason: Uncommitted test changes carry a burden of proof and are removable by default, while production-code changes represent requested behavior and are preserved unless the diff adds unjustified complexity; combining those opposite dispositions would degenerate into a justification framework. Self-grading inherits primacy/self-consistency biases; a rubric-only review is the cheapest fix. Review reports now keep all candidate findings visible by recommended verdict, while fix workflows verify and fix `Recommended Accept` findings directly.
    Tradeoffs: Smaller skill catalog and fewer root-skill re-reads, but direct invocation of cleanup passes goes through `clean-and-fix-code` and orchestrators duplicate a small accepted-finding fix contract instead of delegating to a shared resolver.

- Decision 2026-05-20 lint-suppression-policy: gosec suppressions inline-with-reason; ST10xx dual-enforces revive `exported`
    Decision: No global `gosec.excludes` in `.golangci.yml`; every suppression is inline `// #nosec <rule> -- <reason>` or path-scoped with reason. Test code tightens to `0o600`/`0o700` where incidental. `staticcheck.checks` re-enables `ST1020`-`ST1022`. `issues.max-issues-per-linter: 0` and `max-same-issues: 0` prevent silent caps.
    Reason: Global excludes hid real findings; dual ST10xx + revive is belt-and-suspenders.
    Tradeoffs: Every new perm-literal callsite must tighten or carry a reason; new exported symbols need canonical Go doc comments.

- Decision 2026-05-21 antigravity-replacement: Replace Gemini CLI and retired Antigravity launcher with agy-backed Antigravity
    Decision: Remove `[agents.gemini]` projection and use `[agents.antigravity]`; write repo-local `.agy/antigravity-cli/` config; launch `agy` with `--gemini_dir=<repo>/.agy`; remove the `al gemini` subcommand entirely in the same release (no deprecation window); expose `al agy` (launcher) and `al probe agy`. Subcommand strings follow the launched binary (`agy`) for consistency with `al claude`/`al codex`; internal Go identifiers, package paths, and `[agents.antigravity]` config key keep the product name "Antigravity".
    Reason: Gemini CLI free/pro/ultra is sunset while `agy` is the verified successor. Repo-local `--gemini_dir` containment avoids the old home-directory trusted-folder write and supersedes the prior Gemini Policy Engine/trust-file decisions. A one-release compatibility alias was rejected because the alias is going away anyway and shipping the breaking rename in the same release that introduces the new client keeps the migration surface to a single upgrade step.
    Tradeoffs: Drops Agent Layer projection for upstream Gemini CLI, its model settings, and the retired Antigravity desktop launcher. Existing scripts that invoke `al gemini` will now fail with cobra's "unknown command" error and must switch to `al agy` in v0.10.2. Antigravity MCP config is written and migrated by `agy` v1.0.0, but runtime MCP registration is still false in the observed probe baseline. Subcommand string (`agy`) does not match the internal client identifier (`antigravity`) used in MCP `clients` lists and `[agents.antigravity]` config — accepted because the surface mismatch is small and the binary-named subcommand keeps the user-facing CLI consistent with the rest of `al`.

- Decision 2026-05-28 cli-skill-catalog: Bare init ships no catalog or workflow bundle; wizard installs optional workflow/catalog content
    Decision: The four optional catalog skills (`tavily-web`, `playwright`, `find-docs`, `dispatch-agent`) live in a wizard-only embedded catalog at `internal/templates/skills-catalog/` keyed by `internal/templates/cli-skills-catalog.toml`. The `playwright` skill invokes the `playwright-cli` binary; its distinct skill id avoids collisions with `@playwright/cli`'s bundled `playwright-cli` skill. Bare `al init` installs no catalog skills, workflow skills, instruction files, memory docs/templates, or marker file; it creates empty `.agent-layer/instructions/` and `.agent-layer/skills/`. (Superseded in part by [wizard-instruction-set-and-development-skills, 2026-08-25]: the workflow-bundle yes/no is replaced by an instruction-set select and a grouped `development-skills` catalog row.) The catalog multiselect copies selected catalog skill dirs and removes deselected catalog ids.
    Reason: Direct precedent is `mcp-catalog-seed-split` (2026-05-07). CLI-wrapper skills require external binaries the user may not have; bundling them by default produced silent breakage. The older alternate-layout flag, marker file, and workflow deletion semantics created a second install mode and a surprising delete path. Wizard workflow-bundle refresh also overwrote managed instructions and replaced skill dirs, so full refresh is now an explicit reinstall/upgrade concern rather than a routine wizard option.
    Tradeoffs: Existing repos keep their catalog skill directories on upgrade (the ownership classifier marks them `catalog_skills_v1`); the v0.15.0 migration renames the legacy `playwright-cli` directory to `playwright`, the v0.16.0 migration renames the legacy `agent-dispatch` directory to `dispatch-agent`, and deselecting a catalog id in the wizard removes that catalog directory after diff-preview confirmation. The wizard can no longer repair or refresh partial workflow-bundle installs once any workflow evidence exists; users must reinstall or use upgrade flows for that.

- Decision 2026-05-28 mcp-catalog-cli-skill-preference: Drop ripgrep/filesystem from the MCP catalog; steer ordinary CLI-backed tools to CLI skills
    Decision: The wizard MCP catalog (`internal/templates/mcp-catalog.toml`) ships only `context7`, `tavily`, `fetch`, and `playwright`; `ripgrep` and `filesystem` are removed. The MCP multiselect screen warns that MCP servers are not the recommended default for ordinary CLI-backed tools (prefer CLI command-based skills — see https://agent-layer.dev/cli-skill-design) and not to enable both an MCP server and a CLI skill for the same tool (e.g. Tavily). Do not re-add CLI-wrapper servers to the catalog.
    Reason: Ordinary CLI tools (repo search, file access) belong in CLI command-based skills, which keep live `--help` as the source of truth and avoid per-server MCP tool-schema overhead and config drift; ripgrep/filesystem also duplicate capabilities most clients already have natively.
    Tradeoffs: Users who still want those servers must hand-author them (supported, and shown as reference config examples in README/site docs). No remaining catalog default exercises the optional `clients` field.

- Decision 2026-05-29 wizard-mcp-disable-in-place: Wizard disables MCP servers in place (never deletes) and has no restore-missing prompt
    Decision: `al wizard` never deletes a `[[mcp.servers]]` block. Both catalog defaults and non-catalog customs (`customMCPServers`, surfaced in a dedicated step after the catalog multiselect) follow one rule: an unselected server that exists in config.toml is kept with `enabled = false`; a selected server is enabled. The only asymmetry is insertion — a selected *catalog default* absent from config.toml is added from the embedded catalog, while a custom server has no template and so can only be kept/disabled. There is no separate "restore missing defaults?" confirm; missing defaults are simply unselected options in the multiselect that are added when selected and left absent otherwise. The custom step is skipped in both flow directions when there are no customs (mirrors `skipEnableLayerStep`); a no-op screen would otherwise trap back-navigation. `EnabledMCPServersTouched`/`CustomMCPServersTouched = false` (profile/`--yes`/programmatic) preserves existing state unchanged.
    Reason: Deleting on disable permanently destroys user-authored definitions (custom servers especially have no catalog template) and made the restore-missing prompt necessary and confusing — it defaulted to "yes" yet the multiselect still showed missing defaults unselected, so the answer was effectively a no-op. Disable-in-place is reversible, consistent across both server kinds, and removes the prompt entirely.
    Tradeoffs: A disabled server lingers as a dead `enabled = false` block until the user removes it by hand; the wizard can re-enable it later. Fully removing a default from config.toml is now a manual edit rather than a wizard toggle.

- Decision 2026-05-31 wizard-feature-disable-agent-specific: Scalar disable toggles store direct `agent_specific` keys; the array-shaped AskUserQuestion toggle is a typed flag
    Decision: The scalar wizard "disable" toggles (Codex browser/computer-use, Codex apps, Claude IDE open-file reading, Claude auto-memory, Claude connectors) write the client-native key straight into the agent's `agent_specific` passthrough — Codex `features.*`, Claude `env.*`/`autoMemoryEnabled`. No typed config field, no `fields.go` entry, no sync changes. A key is written only when the user opts in (disable intent); leaving a toggle off omits the key so the client keeps its own default (no hidden defaults). Patch writers `applyCodexBrowserUpdate` and `applyClaudeAgentSpecificUpdate` parallel `applyCodexAppsUpdate`. The Claude AskUserQuestion toggle is the deliberate exception — see [[askuserquestion-deny-plus-hook-default-allowed]].
    Reason: Generated client files already derive entirely from `agent_specific` (Claude deep-merge, Codex append), so a typed field would create a second canonical source and duplicate sync logic. This holds for scalar values. It breaks for the AskUserQuestion toggle, whose value is array-shaped (`permissions.deny` + `hooks.PreToolUse`) and must coexist with user entries — which the line-based wizard patcher cannot union, only clobber. Supersedes the BACKLOG `disable-client-memory-config` proposal of a single translated flag.
    Tradeoffs: The feature→client-key mapping lives in wizard code and CONTEXT.md rather than a typed schema; each new client's controls must be added the same way. Codex `memories` is intentionally not pinned (experimental, off by default).

- Decision 2026-05-31 askuserquestion-deny-plus-hook-default-allowed: AskUserQuestion blocked via a typed flag + sync-time deny/hook injection; template no longer disables it by default
    Decision: The shipped template no longer seeds `permissions.deny = ["AskUserQuestion"]`, so fresh `al init` allows the tool (Claude Code's native default). The wizard's opt-in toggle writes a typed `agents.claude.disable_question_tool = true` scalar (a `*bool`, like `local_config_dir`; no `fields.go`/validation entry needed). When true, `buildClaudeSettings` (`internal/sync/claude_question_tool.go`) injects `permissions.deny += "AskUserQuestion"` (array-union, dedup) and appends the `PreToolUse` hook (`matcher = "AskUserQuestion"`, command exits 2; dedup by matcher) into the generated `.claude/settings.json`, AFTER the agent_specific merge so user-supplied deny/hooks are unioned, never replaced. The hook is always emitted regardless of `approvals.mode`.
    Reason: `permissions.deny` is skipped entirely under YOLO/`bypassPermissions` (Claude Code permission-modes docs), so the deny alone silently fails under YOLO; the PreToolUse hook fires in every mode and is the robust block. The earlier design wrote the deny/hook directly into `agent_specific` via the line-based wizard patcher, which clobbered co-listed user entries (replace on enable; whole-line comment-out on default-accept). A typed flag + structured sync injection is the only way to union with user entries, since the patcher cannot edit array elements. Defaulting to allowed avoids shipping a hidden behavior.
    Tradeoffs: New installs lose the previous default-off behavior (CHANGELOG call-out); existing repos keep their deny (config.toml is never overwritten on upgrade) and migrate via `al wizard`. The injection logic is a small Claude-sync-only special case (the scalar toggles in [[wizard-feature-disable-agent-specific]] need none). Unreleased when introduced, so no migration from the old agent_specific form was required.

- Decision 2026-06-01 wizard-feature-toggles-multiselect: Per-agent feature toggles are two positive-framed multi-selects, not six disable confirms
    Decision: The model step renders one Claude multi-select and one Codex multi-select instead of six individual "Disable X?" yes/no prompts. Each checkbox uses positive framing (checked = keep the feature enabled; unchecking disables it) and is pre-checked from current config so a no-edit re-run produces no config churn. Inversion between checkbox state and the disable-sense storage happens only at the prompt boundary (`promptFeatureToggles` in `helpers.go`); the disable-sense `Choices` fields, their `*Touched` flags, the config keys, and all of patch.go stay the canonical source of truth and are unchanged. Codex apps remains the enabled-sense exception (`featureToggle.enabledSense`) that always writes an explicit `features.apps` per [[wizard-feature-disable-agent-specific]]. Option labels are short, stable strings defined once in `messages` and reused on both the option-list and contains-check sides (the label is the load-bearing match identity); per-feature explanations live in the multi-line group title because `MultiSelect` has no per-option description. The MCP per-server enable/disable flow stays separate.
    Reason: Six sequential disable confirms with inverted ("Yes = disable") phrasing were noisy and easy to misread; two positive-framed checkbox groups are scannable and make the current state visible at a glance. Inverting only at the boundary avoids a second canonical source while delivering the friendlier UX.
    Tradeoffs: A label edit must stay byte-identical between the message constant and the invert match or pre-selection silently breaks (mitigated by single-definition reuse + a round-trip/inversion test). Esc on the Codex multi-select still discards the Claude selection too (the whole model step is one back unit, unchanged).

- Decision 2026-06-30 provider-statusline-sources: Statusline config is explicit; wizard defaults absent keys on
    Decision: `agents.claude.statusline` and `agents.codex.statusline` are explicit booleans for sync/install/upgrade paths: absent or explicit `false` disables, explicit `true` enables. In `al wizard`, absent keys precheck the provider feature toggles while explicit `false` remains unchecked and explicit `true` remains checked. Claude uses `.agent-layer/claude-statusline.sh` projected to `.claude/claude-statusline.sh`; legacy `.agent-layer/statusline.sh` migrates write-if-missing. Codex uses `.agent-layer/codex-statusline.toml` as a statusline-only `[tui].status_line` fragment merged into `.codex/config.toml`, unless `agents.codex.agent_specific.tui.status_line` is explicit. Sync never seeds missing sources; wizard and upgrade seed missing enabled sources once. Existing sources are replaced only through interactive upgrade diff review; non-interactive upgrade skips replacements. The `0.11.0` `config_set_default` ops for both keys ship `value: false`, so a non-interactive `al upgrade --yes` writes the explicit disabled default and enables/seeds nothing; interactive `al upgrade` still prompts defaulting off.
    Reason: A single unified statusline schema made end-user customization too hard and would cap Claude-specific script flexibility. The wizard is an interactive consent point, so defaulting absent keys on improves setup without changing bare init or non-interactive upgrade behavior; explicit `false` remains the durable opt-out. Defaulting migrations to `false` keeps `--yes` minimal (no unexpected feature enablement, no settings mutation). Boolean toggles keep Agent Layer responsible for provider wiring while users edit each provider-native source directly.
    Tradeoffs: Default-accepting wizard runs can now enable and seed statusline sources when the provider is enabled and the config key is absent. Hand-edited configs with `statusline = true` fail loudly when the source is missing until the user runs `al wizard`, interactive `al upgrade`, or creates the source manually. Shipped source improvements reach only missing sources or existing sources accepted through interactive upgrade review. Codex cannot mirror Claude script-only fields such as ANSI formatting, cost, lines changed, or worktree label.

- Decision 2026-06-30 release-artifacts-immutable: Tagged release manifests are immutable
    Decision: After a version is tagged, do not modify its `internal/templates/manifests/<version>.json` or `internal/templates/migrations/<version>.json` for later work. Create new manifest and migration files for the forthcoming release version instead, even when the migration file has no operations.
    Reason: These files define the published ownership baseline and upgrade contract for that tag. Rewriting them after release makes the source tree disagree with the released artifact and can change direct-upgrade behavior for historical target versions.
    Tradeoffs: Patch releases may carry empty migration manifests and duplicate-looking release artifacts, but historical upgrade behavior remains auditable and byte-stable.

- Decision 2026-06-30 antigravity-model-first-class: Antigravity model uses typed config, not agent_specific
    Decision: `agents.antigravity.model` is the only supported Antigravity model field. Sync projects it to generated Antigravity `settings.json`; runtime validation rejects `agents.antigravity.agent_specific.model`. The v0.12.0 upgrade migration moves that passthrough key to the typed field before validation.
    Reason: Wizard, dispatch, field catalog, docs, and defaults all treat Antigravity model selection as an Agent Layer-owned concept. Keeping it under `agent_specific` made a known field look like unmanaged passthrough and created two possible sources of truth.
    Tradeoffs: Fresh hand-authored configs that put `model` under `agent_specific` fail loudly and must move the value to `agents.antigravity.model`; upgrading repos get the automatic move first.

- Decision 2026-07-01 codex-local-home-opt-in: Repo-local Codex home requires explicit opt-in
    Decision: Gate `CODEX_HOME=<repo>/.codex` behind `[agents.codex] local_config_dir = true` for `al codex`, Codex dispatch, and `al vscode`; absent/false preserves inherited `CODEX_HOME`.
    Reason: Agent Layer should keep generating repo-local Codex project config while leaving Codex's normal global/project config layering as the default. Users who want per-repo Codex auth, sessions, logs, and runtime state can opt in.
    Tradeoffs: Existing users who relied on automatic per-repo Codex auth must set `agents.codex.local_config_dir = true`; default mode shares Codex home state but better matches user-managed Codex workflows.

- Decision 2026-07-02 codex-config-shared-state: `.codex/config.toml` is shared state patched by sync
    Decision: `al sync` patches only known Agent Layer-owned Codex entries in `.codex/config.toml`, preserves unrelated Codex/user runtime entries, and seeds current repo project trust only when the exact project entry is absent.
    Reason: Codex writes durable runtime/project state into the same file Agent Layer must refresh for model, approval, statusline, feature toggles, and MCP projection.
    Tradeoffs: Invalid existing TOML and shape conflicts now block sync instead of being overwritten; arbitrary removed passthrough paths outside known Agent Layer-owned paths may require manual cleanup.

- Decision 2026-07-02 cli-exec-handoff-signing: Agent launch exec handoff plus signed binary delivery
    Decision: Interactive CLI launchers (`al claude`, `al codex`, `al copilot`, `al agy`) replace `al` with the target agent via `syscall.Exec`; darwin release binaries are Developer ID signed/notarized as `com.conncastle.agent-layer`; Homebrew renders a binary formula for release assets instead of building from source.
    Reason: macOS TCC/keychain prompts must attribute protected access to the actual agent binary, and grants should survive Agent Layer upgrades for install-script and Homebrew users.
    Tradeoffs: Agent exit codes and stderr now pass through without Agent Layer's wrapper; users get a one-time permission prompt under each agent identity, while stale `al` grants remain cosmetic.

- Decision 2026-07-10 antigravity-settings-overlay-preserve: Antigravity settings.json is overlay-patched, never delete-on-absent
    Decision: WriteAntigravitySettings overlays Agent Layer's projected keys (model, permissions.allow, agent_specific — all from buildAntigravitySettings, the single managed-path source) onto the user's native .agy/antigravity-cli/settings.json and preserves every native key it does not produce; a managed key absent from config is left in place, not deleted. Empty/whitespace files are treated as missing, the existing file mode is preserved (new files 0o600), and malformed/non-object/shape-conflict native state fails loud before any write. Native content is parsed as strict JSON, matching Antigravity CLI 1.1.3.
    Reason: settings.json is owned by Antigravity and the user (its /config menu persists selections such as model there), so an omitted Agent Layer value is not an instruction to erase native state, and delete-on-absent is an unsafe contract a future Antigravity release could break. Isolated runtime probes using absolute `--gemini_dir` paths proved that Antigravity CLI 1.1.3 rejects line comments, block comments, and trailing commas with failed-to-unmarshal errors; a strict-JSON control loaded without error.
    Tradeoffs: A managed value removed from config.toml (or left when Antigravity is disabled) lingers in the file, indistinguishable from native. Antigravity silently falls back to defaults when settings parsing fails and still exits successfully at the command surface; Agent Layer deliberately fails loud before writing so invalid native state is not silently discarded. Extends the shared-state-patch pattern of [[codex-config-shared-state]].

- Decision 2026-07-28 single-level-parallel-plan-review: Plan work owns external review and finding resolution
    Decision: Keep `dispatch.max_depth = 3` for custom workflows, but use one Agent Dispatch level for built-in plan review. `/plan-work` resolves supplied reviewers through Agent Dispatch metadata and dispatches them concurrently. Each `/review-plan` reviewer is an Agent Dispatch leaf that directly reviews the complete artifact set through 1–3 risk-based framings and returns only its evidence-backed material findings. `/plan-work` independently accepts or rejects every finding, justifies rejections, addresses accepted findings in the artifacts, and then writes the final report.
    Reason: The planner owns the artifacts and final judgment, separate dispatched reviewers provide independent provider contexts, and direct framings broaden each review without nested orchestration.
    Tradeoffs: Multiple framings in one reviewer are not independent reviews; callers need multiple `plan_reviewers` when that independence matters. Findings are still validated by the reviewer and planner, adding effort but preventing weak conclusions from becoming plan changes.

- Decision 2026-07-13 code-reviewer-dispatch-role: Semantic code review is a required `code_reviewer` dispatch leaf
    Decision: `full-workflow`, `fully-implement-plan`, and the planned path of `debug-and-fix-issue` require a `code_reviewer` dispatch target and run `/review-uncommitted-code` through it; `/verify-work` stays a built-in fresh subagent. Lighter fix-inline shapes (`improve-codebase`, `clean-and-fix-code`) keep the built-in reviewer.
    Reason: Cross-provider reviewer diversity against the implementer and root session is a deliberate quality property, not an accident of target configuration; code review is a bounded non-interactive leaf, the shape dispatch is reserved for.
    Tradeoffs: Callers must supply one more dispatch target and pay dispatch latency per review cycle; verify-work intentionally stays same-model built-in, so contract verification does not gain provider diversity.

- Decision 2026-07-14 global-notifications-chime: One filtered handler serves project-shareable provider hooks
    Decision: `notifications.chime` remains the single typed opt-in; sync projects `al hook chime <provider>` into ordinary Claude, Codex, Agent Layer-owned Antigravity, and Grok `Stop` hooks, where typed event filters accept only known top-level, non-background, normal completion states and every projected command fails open. Grok's hook lives in `.grok/hooks/agent-layer-chime.json` because project `.grok/config.toml` does not load hook tables.
    Reason: One handler prevents duplicate continuation/background/error chimes while preserving user hooks, shared runtime state, machine-independent project artifacts, and the existing no-global-provider-ownership boundary; it supersedes direct provider-specific `afplay` commands.
    Tradeoffs: Sound uses the native macOS `afplay` command or, on Linux, `canberra-gtk-play --id=complete` when available; Agent Layer bundles no audio asset, so Linux systems without that command remain silent. The handler requires `al` on the provider process `PATH`; Codex runs matching hooks concurrently, so its first `Stop` can precede another hook's continuation request and cannot prove final semantic completion. Grok folder trust is seeded into repo-local `.grok-config/trusted_folders.toml`; an existing exact folder entry is preserved.

- Decision 2026-07-17 claude-lineage-derived-proof: Claude lineage capture is version-gated, append-only evidence
    Decision: Keep Claude 2.1.207 as the tested dispatch baseline, gate `--forward-subagent-text` and normalized lineage capture separately at 2.1.211, and store one append-only `provider.lineage` artifact per run. The asynchronous cutover removed `inspect`/`history` and their derived descendant summaries; the normalized evidence remains the provider-authoritative source for any future consumer.
    Reason: Provider-authoritative relationships and task notifications can prove a bounded Claude lineage, while mutable summaries, raw-output reconstruction, or claims for providers without equivalent events would create competing truth or inferred terminality.
    Tradeoffs: Missing, malformed, conflicting, or discontinuous evidence remains explicitly unknown; old and unsupported-provider records require no migration but cannot gain proof retroactively; no shipped command currently reads the artifact.

- Decision 2026-07-17 secret-url-query-key-segments: Secret-like URL query keys use identifier segments
    Decision: Match secret-like URL query keys by separator, camelCase, and acronym segments instead of arbitrary substrings.
    Reason: The user chose to remove false positives such as `author`, `authority`, `tokenizer`, and `passwordless` while retaining common segmented secret-key forms.
    Tradeoffs: Glued lowercase keys such as `authtoken`, `accesstoken`, and `clientsecret` are intentionally not detected.

- Decision 2026-08-03 dispatch-headless-approvals: `approvals.mode` is delivered to headless dispatch on the command line
    Decision: `al dispatch` sends the approval policy as provider flags instead of relying on generated config. Non-YOLO Codex dispatches set `sandbox_mode` (`read-only` for `none`/`mcp`, `workspace-write` for `commands`/`all`); Claude dispatches set `--permission-mode` (`dontAsk` / `acceptEdits`) plus the command and MCP allowlist as repeated `--allowedTools` flags. YOLO is unchanged. Editing capability keys off `projection.Approvals.AllowCommands`, and a non-empty Claude `permission_denials` fails the dispatch.
    Reason: `codex exec` defaults to a read-only sandbox (the interactive TUI defaults a version-controlled folder to `workspace-write`), and `claude -p` denies Edit/Write, so every non-YOLO dispatch was unable to do implementation work. Claude also ignores a project's `permissions.allow` rules under `-p` until the workspace trust dialog is accepted, which never happens headlessly, so the generated settings file could not carry approvals at all. Codex has no separate edit-approval rule, so its sandbox is the edit gate and must track command approval rather than being set independently.
    Tradeoffs: Dispatch and interactive launches now differ for non-YOLO modes; interactive keeps provider defaults because a human can approve escalations. `workspace-write` still protects `.git` and denies network including loopback, so dispatched agents cannot commit or bind test servers without explicit `agent_specific` passthrough. Failing on any denial also fails runs where the agent recovered via another route, which is accepted because a headless run has no one to course-correct and a denial means approvals were misconfigured.

- Decision 2026-08-04 skill-import-repository-credentials: Skill import repository URLs reference secrets by placeholder, never literally
    Decision: `repository` and `push_repository` may carry `${AL_*}` placeholders anywhere, including in URL userinfo and query values, matching how MCP server URLs and headers reference secrets. `skilllock.ValidateRepository` — called by both configuration and lockfile validation — is the single boundary, and rejects every literal form a URL can carry a credential in: a password component on any scheme, userinfo on any scheme outside the identity-only allowlist (`ssh`, `git`), a value under a credential-bearing query key, and any placeholder outside the `AL_` namespace that `.agent-layer/.env` is filtered to. A scheme that is itself a placeholder is treated as possibly web, because its resolved value cannot be checked. Placeholder text stays canonical in configuration identity, in `.agent-layer/skills.lock.json`, and in status and report output. Resolution happens only in `gitrepo.Secrets`, at the Git access boundary: `gitrepo.Repository` carries the configured text and the resolved value separately, its `String` returns the configured text so every message is safe by construction, and every value it substitutes is redacted back to its placeholder in `CommandError` arguments and stderr. `internal/envref` owns both halves of the shared vocabulary — the `${NAME}` syntax and the credential-bearing query keys — so the substitution path, the MCP policy warning, and repository validation cannot drift into separate definitions of what counts as a secret.
    Reason: The URL is written into `config.toml`, copied into the lockfile, printed by `al skills status`, and joined into every failed Git command's error message, because a repository URL is an ordinary command argument that Git echoes into its own diagnostics. Redacting at each of those sites would have to stay exhaustive as messages are added, so the resolved value is instead confined to one boundary and the recorded form never contains it. Refusing literals outright keeps the secret out of Agent Layer's state entirely, which is what `internal/gitrepo`'s contract already claimed. An SSH username is an identifier rather than a secret, so `git@host:org/repo.git` and `ssh://git@host/path` stay accepted literally. The scheme allowlist is deliberately closed rather than open: an unknown or placeholder-built scheme with literal userinfo is refused, because assuming it is not web would let `${AL_SCHEME}://token@host` hand Git `https://token@host` while passing validation.
    Tradeoffs: A user with a working `https://user:token@host/repo.git` must move the credential into `.agent-layer/.env` as an `AL_` key, or into a Git credential helper or SSH — a deliberate breaking constraint on an unreleased feature, chosen over persisting the secret. A literal username on an exotic-but-legitimate transport is also refused; the allowlist can be widened when a real one appears, which is cheaper than the reverse. Because identity keys off placeholder text, two blocks spelling the same destination differently are different push groups. Redaction replaces every occurrence of a resolved value, so a value that is also ordinary text (a short word, a common branch name) would be over-redacted in output; over-redaction is the safe direction and no length heuristic is applied.

- Decision 2026-08-04 skill-import-project-lock-serialization: Remote skill operations hold the project lock for their complete duration
    Decision: Fetch, pull, push, reset, local skill mutations, and projection retain the existing project lock from the initial state read through every remote operation and local publication. No snapshot fingerprint, optimistic revalidation, or remote-work-outside-lock path is added for the first release.
    Reason: One serialized mutation model avoids a remote success followed by failed local revalidation and requires no parallel state or recovery protocol. The simpler consistency boundary is preferred until observed operation timing demonstrates a real availability problem.
    Tradeoffs: A slow remote can make concurrent sync or agent launch wait or time out behind the project lock. That availability cost is accepted for the first release and must be reconsidered only from operational evidence in a separately requested change.

- Decision 2026-08-06 disposable-skill-roots-require-no-claim-preflight: Init and upgrade never inspect client skill root contents for ownership
    Decision: `.agents/skills/` and `.claude/skills/` require no ownership preflight during init or upgrade. The published `v0.16.0` `claim_skill_projection_roots` migration operation remains supported as a compatibility no-op.
    Reason: Agent Layer already defines both roots as exclusively owned disposable projections and sync replaces them wholesale. Blocking on content inside them contradicted that contract and made derived state capable of preventing an upgrade.
    Tradeoffs: Init or upgrade followed by sync can delete independently installed content from these roots without a special warning. That is the documented behavior; durable skills must live in `.agent-layer/skills/` or `.agent-layer/skills-imported/`.

- Decision 2026-08-07 imported-skills-are-committed-project-state: Imported skills and their lockfile are committed, not machine-local cache
    Decision: The generated `.agent-layer/.gitignore` no longer ignores `skills-imported/` or `skills.lock.json`; both are committed project state. Only `skills-imported/.staging/`, the import transaction's scratch space, stays ignored. The two are inseparable: `sync.verifyImportedOwnership` treats an imported directory with no lock entry as a loud orphan, so ignoring one while committing the other breaks `al sync` on every fresh clone.
    Reason: Treating the imported tier as a rebuildable cache left a fresh clone or CI run with `[[skills.imports]]` blocks but no skill content until someone ran a networked (and, for private sources, credentialed) `al skills pull`. Imported skills stay editable by design and `pull` preserves local edits, which makes those edits durable project content that no other mechanism can share or review. A `pinned` import only actually reproduces if its resolved content travels with the repo.
    Tradeoffs: Vendored skill trees enlarge diffs, and concurrent `pull`s can conflict in the lockfile — accepted because both are ordinary lockfile costs and both are reviewable. Because `.agent-layer/.gitignore` is agent-owned and rewritten unconditionally, existing projects pick this up without a migration but must `git add` the two paths once to begin tracking them. The root `.gitignore` block still ships `/.agent-layer/` uncommented, so this only takes effect for projects that opt into committing `.agent-layer/`; changing that default is a separate decision.

- Decision 2026-08-07 skill-push-publication-checkpoints: Contribution pushes keep destination history separate from source locks
    Decision: A successful push to a destination other than the exact tracked source ref records a per-skill publication commit and tree hash in skill lock schema version 2. Later pushes to that repository and branch use it as their merge base; pull and reset preserve it. Version 1 locks remain readable, but every subsequent lock write emits version 2. A deleted or rebased-away destination discards its unusable checkpoint and falls back to the immutable source base. A checkpoint-less merge that would silently retain a non-empty destination instead of the requested local tree fails explicitly.
    Reason: The source lock must remain the upstream merge base for pull and local-status semantics, while repeated review pushes need the last result Agent Layer actually published to distinguish follow-up edits and reversions from destination changes. A pre-checkpoint branch cannot prove which changes Agent Layer made, and a missing historical commit supplies no trustworthy destination base, so conservative source reconciliation is safer than inference.
    Tradeoffs: Every consumer of a committed version 2 lock must upgrade. The first push against a branch created before checkpoints existed can conflict or reject an ambiguous reversion and may require aligning local content with the branch once to establish a checkpoint. A remote push followed by a failed local lock write can still lose the checkpoint; the next push then returns to conservative source reconciliation.

- Decision 2026-08-17 grok-home-isolation: Repo-local Grok auth uses `.grok-config/`, not project `.grok/`
    Decision: `al grok`, Grok dispatch, and `al vscode` always set `GROK_HOME=<repo>/.grok-config`. Agent Layer writes generated MCP, permission, and plugin output to `.grok/config.toml`, and seeds folder trust in `.grok-config/trusted_folders.toml` only when that exact repo entry is absent and `.grok-config` is a real directory rather than a symlink. Agent Layer creates the home at `0700` before the Grok CLI can mkdir it, and tightens an existing real home that grants group or other access to `0700`. Create and chmod of that final component use a parent-directory handle with no-follow open so a swapped symlink cannot redirect them. Symlinks and non-directories still fail.
    Reason: Grok stores user `config.toml` and `auth.json` under `GROK_HOME`. Pointing `GROK_HOME` at project `.grok/` would collide with the generated project config. Always isolating home is what makes folder-trust seeding possible without writing `~/.grok`. The Grok CLI creates a missing `GROK_HOME` with default `0755`, so rejecting those permissions asked users to chmod a directory Agent Layer owns.
    Tradeoffs: Two Grok directories instead of one. Each repo starts with its own Grok auth/home state and does not inherit `~/.grok` plugins or memory unless the user copies them. Tightening an existing home is silent; a user who intentionally shared `.grok-config` with group/other will have that access removed on the next sync or Grok launch.

- Decision 2026-08-20 benchmark-empty-dispatch-roles-identity: Explicit-empty dispatch roles keep unconstrained experiment identity
    Decision: Skills experiments must declare `required_dispatch_roles`. An explicit `[]` is stored as nil in experiment identity (`omitempty`) and in `TreatmentManifest.RequiredRoles`, so unconstrained JSON remains `required_dispatch_roles: null` and historical unconstrained cells stay reusable. Nonempty normalized roles participate in both identities.
    Reason: Omitting the field would silently disable enforcement, but encoding explicit empty as `[]` would change every existing unconstrained arm and manifest hash.
    Tradeoffs: Presence of the TOML key is a validation concern distinct from identity bytes. Constrained studies cannot reuse older unconstrained results whose stored conformance flag was true.

- Decision 2026-08-20 benchmark-dispatch-slot-skill-match: Conformance matches lifecycle skill to each role slot
    Decision: Required dispatch slots match completed fresh-root Agent Dispatch records one-to-one on the role's workflow skill plus the configured agent, model, and reasoning effort. `RunRecord` has no role field; `plan-reviewer` maps to `review-plan`, `implementer` to `implement-plan`, and `code-reviewer` to `review-uncommitted-code`. Records that omit `skill` cannot fill a constrained slot. Unconstrained skills experiments still skip slot matching.
    Reason: Default treatments assign the same execution target to every role, so target identity alone cannot prove the declared protocol ran.
    Tradeoffs: Prompt-only or skill-less historical dispatches are nonconformant for nonempty role contracts even when agent, model, and effort match.

- Decision 2026-08-30 benchmark-dispatch-role-attribution: Conformance uses an explicit dispatch role, superseding skill-based role inference
    Decision: `dispatch_start` accepts an optional caller-defined `role` and retains it on `RunRecord`. Required benchmark slots match completed fresh-root records one-to-one on that exact role plus the configured agent, model, and reasoning effort. The benchmark contract supplies `plan-reviewer`, `implementer`, and `code-reviewer` as explicit tool fields; prompt text is not evidence. This supersedes `benchmark-dispatch-slot-skill-match` for new constrained studies.
    Reason: The consolidated `implement` workflow deliberately dispatches self-contained prompts and removed the old `review-plan`, `implement-plan`, and `review-uncommitted-code` wrapper skills. Requiring those deleted skill names made the benchmark's declared workflow impossible to satisfy and silently classified real plan, implementation, and review dispatches as nonconformant.
    Tradeoffs: `dispatch_start` gains one small optional schema field. Historical constrained records without `role` remain nonconformant rather than being reclassified from prompt text; unconstrained records remain unaffected.

- Decision 2026-08-20 grok-claude-md-compat: Claude instructions live under `.claude/`; Grok Claude-agents discovery is off
    Decision: `al sync` writes the Claude instruction shim to `.claude/CLAUDE.md` and deletes a generated root `CLAUDE.md`. When Grok is enabled, sync sets `[compat.claude] agents = false` in repo-local `$GROK_HOME/config.toml`, and `al grok`, Grok dispatch, and `al vscode` set `GROK_CLAUDE_AGENTS_ENABLED=false`. Shared instructions stay in root `AGENTS.md`.
    Reason: Grok 1.0.5 loads every matching root instruction name, so identical `AGENTS.md` and `CLAUDE.md` were injected twice. The Claude compat cell does not suppress root `CLAUDE.md`, but it does suppress `.claude/CLAUDE.md`. Claude Code accepts either project location.
    Tradeoffs: A leftover hand-authored root `CLAUDE.md` still dual-loads in Grok. Agent Layer launches no longer discover Claude-named files under `~/.claude/` or `.claude/CLAUDE.md`. The compat key is merged into user-owned GROK_HOME config.

- Decision 2026-08-25 wizard-instruction-set-and-development-skills: Wizard instructions and development skills are independent
    Decision: `al wizard` asks for always-on instructions as None / Rules / Rules and memory, then lists skills in one catalog including a grouped `development-skills` row for `implement`, `ship-pr`, `auto-skill-loop`, and the audits. Fresh init defaults to Rules and memory; catalog rows, including development skills, default from disk. Instruction seeding is install-only and skipped when standard or legacy managed instruction files, live memory docs, or memory templates already exist. User-authored extra instruction files do not skip the prompt. Development skill directories do not skip the instruction prompt. Selecting the grouped row adds or repairs member directories from `internal/templates/skills/`; deselecting removes those member directories. Members stay upgrade-managed workflow templates, not `catalog_skills_v1`.
    Reason: The old workflow-bundle yes/no mixed always-on policy, project memory, and optional workflows, so users could not take rules without `implement` or memory without the skill pack. A 3-way instruction choice and one catalog checkbox keep those products independent without a second skills screen.
    Tradeoffs: Unchecking development skills deletes the seven member directories after preview confirmation, including customized copies at those names. Existing repos with any instruction or memory file still cannot add the other instruction tier through the wizard; that remains an upgrade or manual copy. Fresh init no longer auto-installs the seven workflow skills.

- Decision 2026-08-27 coverage-is-diagnostic: Coverage reporting is not a pass/fail quota
    Decision: `make coverage` always reports per-file and total coverage but does not enforce a percentage threshold. `make ci` retains the report while test acceptance is based on behavioral contracts and the complete verification lanes.
    Reason: The former numeric gate incentivized thousands of lines of private-branch, error-injection, and implementation-shaped tests while still missing a realistic Antigravity success event at the public dispatch boundary.
    Tradeoffs: Coverage can decline without directly failing CI; reviewers must assess whether changed behavior remains protected instead of treating a global percentage as proof of test quality.

- Decision 2026-08-28 global-update-follows-install-owner: Global CLI updates follow the current installation method
    Decision: `al update` bypasses repository version dispatch. If Homebrew identifies the running release executable as part of the Agent Layer formula, it runs `brew upgrade conn-castle/tap/agent-layer`; otherwise it downloads the official installer to a temporary file and runs it with the prefix derived from `<prefix>/bin/al`. Development builds fail explicitly. Update guidance uses `al update`; `al upgrade` remains the separate repository migration workflow.
    Reason: Static Homebrew-first guidance was misleading on script-installed Linux hosts, and users should not need to remember which machine used which supported installer. Operating on the invoking global binary prevents a repository pin from updating the wrong executable.
    Tradeoffs: A manually renamed or nonstandard release executable cannot supply a safe prefix and fails. A binary visibly under a Homebrew Cellar also fails if Homebrew cannot identify it as the active formula, rather than writing into package-manager-owned storage. Script-based updates skip the installer's completion refresh so a nested completion command cannot select a repo-pinned binary; existing completions remain usable and can be refreshed explicitly.

- Decision 2026-09-01 benchmark-published-proxy-inference: One-repetition studies may use labeled published_proxy inference
    Decision: Newly exported DeepSWE selections are schema v3 and pin per-task published configuration identity, sample size, sample variance, and the unbiased n-1 variance convention. Pairwise reports prefer observed Welch inference when every required task has at least two completed repetitions in both arms; otherwise they use `published_proxy` when every required task has complete pinned published evidence. The proxy shares one published variance estimate across both arms (`combined = c² s² (1/rL + 1/rR)`, one Satterthwaite term per task) rather than treating it as two independent estimates. Historical v1/v2 selections remain valid and do not gain proxy p-values. Selector-versus-executed model/reasoning mismatches are permitted and warned. Report generation never fetches planner data.
    Reason: Requiring two executed repetitions made one-repetition studies unable to report a p-value even when the planner already had complete published cell variances. Sharing the published variance avoids the anti-conservative duplicated-independent degrees of freedom.
    Tradeoffs: Proxy p-values estimate uncertainty from published runs, not from the executed repetitions, and a mismatched executed model still uses the selector's published variance. Observed inference remains preferred when both arms have at least two repetitions.

- Decision 2026-09-07 dispatch-invocation-termination-evidence: Invocation IDs stay immutable; termination proof is independent of outcome
    Decision: Public dispatch results expose the existing `RunRecord.ID` as `invocation_id`. Continue creates a new ID; an explicit old ID never follows the conversation. `termination_confirmed` is monotonic and independent of cancelled/failed/completed; public inspection exposes only confirmation and its timestamp. Provider launch writes durable launch intent under the run lock before `cmd.Start`; a crash between spawn and identity publication remains uncertain and is never treated as proof that no provider exists. Evidence updates after a terminal outcome use a dedicated path that cannot erase cancellation. Output is limited to bounded UTF-8 `final_answer` and `events` text. Confirmed terminal evidence and inactive mappings expire after 30 days; unconfirmed records never expire.
    Reason: Callers need to address one invocation, tell a cancel request apart from a proven stop, and retrieve partial output without reading Agent Layer internals. A run lock is not proof no child exists once `cmd.Start` has returned.
    Tradeoffs: Older binaries that reject unknown run-record fields cannot read new records. Unconfirmed or pre-protocol records can accumulate rather than being guessed expired. Public callers cannot retrieve raw provider diagnostics or inspect the private termination proof basis.
