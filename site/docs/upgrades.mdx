---
title: Upgrades
description: Upgrade policy, compatibility guarantees, migration rules, and platform support.
sidebar_position: 8
---

This page defines how Agent Layer upgrades work.

It covers:
- upgrade event categories,
- compatibility guarantees,
- release-versioned migration rules,
- and the supported OS/shell capability matrix.

If guidance in other docs is shorter or summarized, this page is authoritative.

## In this page

- [How to upgrade](#how-to-upgrade)
- [Upgrade event model](#upgrade-event-model)
- [Compatibility guarantees](#compatibility-guarantees)
- [Ephemeral artifacts and rollback scope](#ephemeral-artifacts-and-rollback-scope)
- [Migration rules by release](#migration-rules-by-release)
- [Documentation version retention and access](#documentation-version-retention-and-access)
- [OS and shell capability matrix](#os-and-shell-capability-matrix)
- [Commitment level and limitations](#commitment-level-and-limitations)
- [Upgrade checklist](./upgrade-checklist)

## How to upgrade

1. Update the global `al` CLI:
   - Run `al update`. It uses Homebrew when Homebrew owns the executable and otherwise reruns the official release installer with the current prefix.
   - If the installed CLI predates `al update`, update it once with `brew upgrade conn-castle/tap/agent-layer` for Homebrew or rerun the original script install command with the same prefix.
2. Upgrade the repo: `al upgrade plan` then `al upgrade`.

For a full step-by-step runbook (interactive and CI), see the [upgrade checklist](./upgrade-checklist).

## Upgrade event model

Agent Layer classifies upgrade-impact changes into three categories:

| Category | Meaning | Expected user action |
| --- | --- | --- |
| `safe auto` | Low-risk change that can be applied automatically without manual migration steps. | Usually none beyond normal verification (`al doctor`, test/lint workflow). |
| `needs review` | Change is likely safe but may interact with local customizations. | Review prompts/diffs and choose whether to apply updates. |
| `breaking/manual` | Change cannot be safely auto-migrated or may require explicit user decisions. | Follow release migration notes before proceeding. |

Qualification guidance:
- `safe auto`: formatting-only docs changes, additive defaults, or internal behavior fixes that do not require user input.
- `needs review`: template updates where local edits may conflict, workflow/instruction rewrites, generated file ownership boundary changes.
- `breaking/manual`: command/flag removals, required config schema moves, or behavior changes that require an explicit user-controlled migration step.

Breaking-change policy:
- Agent Layer uses clean breaks for breaking/manual changes. Removed commands/flags fail immediately in the target release.
- We document exact migration actions in release notes and migration guidance instead of keeping compatibility shims.

Current enforcement status:
- This model is the published contract and release-labeling policy.
- Automated enforcement in CLI flows is planned for a future release.

## Compatibility guarantees

Agent Layer follows a sequential release-line guarantee:

- Guaranteed path: upgrade from `N-1` to `N` release lines only.
- Example: `0.7.x` to `0.8.x` is guaranteed.
- Non-guaranteed path: skipping release lines (for example `0.6.x` to `0.8.x`).

What "guaranteed" means:
- We provide documented migration guidance for the guaranteed path.
- We classify known upgrade-impact changes using the event model on this page.
- We aim to keep `al upgrade` workflows predictable for that one-step path.

What it does not mean:
- It is not a guarantee that every repo custom state can be auto-migrated.
- It does not guarantee one-step upgrades for skipped release lines.

## Repo-local upgrade keep list

When an interactive upgrade finds unknown paths under `.agent-layer/` or `docs/agent-layer/`, it offers to record intentional local paths in `.agent-layer/upgrade-keep-list` before continuing to the existing deletion flow. The checklist lists `.agent-layer/tmp` last and unchecked, rather than as individual tmp files. Kept files and directory subtrees are omitted from future upgrade plans and deletion decisions, including non-interactive `--apply-deletions` and `--apply-tmp-deletions`. Listing `.agent-layer/tmp` keeps the whole directory and skips the grouped tmp deletion prompt. Individual files under tmp cannot be listed. Keeping a nested file does not keep sibling paths under the same parent. The gitignored file uses one exact repo-relative path per line, supports blank lines and `#` comments, does not expand globs, and is never pruned automatically. It does not suppress managed-template updates. Tmp deletion otherwise retains its separate double-confirm cleanup flow.

## Ephemeral artifacts and rollback scope

`.agent-layer/tmp/` is a scratch directory for agent run artifacts (plans, reports, scratch dumps). It is treated specially across the upgrade lifecycle:

- **Snapshots exclude tmp.** Pre-upgrade snapshots under `.agent-layer/state/upgrade-snapshots/` do not capture files under `.agent-layer/tmp/`. This keeps snapshot size bounded regardless of how much scratch state has accumulated.
- **Rollback does not restore tmp.** `al upgrade rollback <snapshot-id>` will not recreate any tmp file deleted during the upgrade. If you need to keep an in-progress agent artifact across an upgrade, copy it out of `.agent-layer/tmp/` first.
- **Tmp deletion is gated.** Interactive deletion requires a destructive double-confirm unless `.agent-layer/tmp` is listed in `.agent-layer/upgrade-keep-list`. Non-interactive deletion requires the dedicated `--apply-tmp-deletions` flag — `--apply-deletions` alone never touches tmp content. The interactive "delete all unknowns?" prompt is similarly scoped: it deletes everything **except** tmp, and the grouped tmp prompt fires separately when tmp is not kept.

If automatic or manual rollback fails, the snapshot is recorded with status `rollback_failed` and remains available for recovery. Resolve the reported filesystem error, then retry `al upgrade rollback <snapshot-id>`. Every retry resets the snapshot's scoped targets before restoring them, so a failure that restored only some entries can safely converge on the captured state. After a successful retry, the snapshot keeps its prior failure details as recovery evidence and is marked `manually_rolled_back`.

For full mechanics see [Ephemeral artifacts under .agent-layer/tmp/](./reference#ephemeral-artifacts-under-agent-layertmp) in the reference guide.

## Migration rules by release

Migration rules are published per release and versioned with docs/release notes.

| Target release | Supported source release lines | Event summary | Required manual actions | Notes |
| --- | --- | --- | --- | --- |
| `v0.19.0` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0`, `0.17.1`, `0.17.2`, `0.17.3`, `0.17.4`, `0.17.5`, `0.17.6`, `0.17.7`, `0.17.8`, `0.17.9`, `0.18.0`, `0.18.1`, `0.18.2`, `0.18.3`, `0.18.4`, `0.18.5`, `0.18.6` | `safe auto`: Adds CLI and MCP dispatch inspection and output tools, invocation selector targeting, and bounded provider process-group termination proof. | Run `al upgrade plan` then `al upgrade`. | The v0.19.0 migration manifest is empty; changes are additive dispatch inspection, output, and termination updates and require no manual migration. It preserves the `0.10.2` migration floor. |
| `v0.18.6` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0`, `0.17.1`, `0.17.2`, `0.17.3`, `0.17.4`, `0.17.5`, `0.17.6`, `0.17.7`, `0.17.8`, `0.17.9`, `0.18.0`, `0.18.1`, `0.18.2`, `0.18.3`, `0.18.4`, `0.18.5` | `safe auto`: Adds client-managed OAuth MCP configuration and explicit doctor status for unvalidated OAuth servers; hardens Agent Dispatch handoff, provider identity and termination proof, and dispatched-child warning output. | Run `al upgrade plan` then `al upgrade`. | The v0.18.6 migration manifest is empty; changes are additive runtime, configuration, and documentation updates and require no manual migration. It preserves the `0.10.2` migration floor. |
| `v0.18.5` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0`, `0.17.1`, `0.17.2`, `0.17.3`, `0.17.4`, `0.17.5`, `0.17.6`, `0.17.7`, `0.17.8`, `0.17.9`, `0.18.0`, `0.18.1`, `0.18.2`, `0.18.3`, `0.18.4` | `safe auto`: Completed benchmark reports regenerate from immutable study evidence without current treatment preflight, upgrade commands reject targets newer than the running CLI, and release builds verify target migration packaging plus artifact-level upgrade behavior. | Run `al upgrade plan` then `al upgrade`. | The v0.18.5 migration manifest is empty; changes harden recovery, upgrade diagnostics, and release validation without requiring manual migration. It preserves the `0.10.2` migration floor. |
| `v0.18.4` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0`, `0.17.1`, `0.17.2`, `0.17.3`, `0.17.4`, `0.17.5`, `0.17.6`, `0.17.7`, `0.17.8`, `0.17.9`, `0.18.0`, `0.18.1`, `0.18.2`, `0.18.3` | `safe auto`: DeepSWE selections can pin published sample variance so one-repetition studies report labeled published-proxy inference when observed Welch variance is unavailable. | Run `al upgrade plan` then `al upgrade`. | The v0.18.4 migration manifest is empty; changes are additive benchmark selection and report inference and require no manual migration. It preserves the `0.10.2` migration floor. |
| `v0.18.3` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0`, `0.17.1`, `0.17.2`, `0.17.3`, `0.17.4`, `0.17.5`, `0.17.6`, `0.17.7`, `0.17.8`, `0.17.9`, `0.18.0`, `0.18.1`, `0.18.2` | `safe auto`: Agent Dispatch records an optional caller-defined workflow role. Benchmark studies can recover evidence-backed verifier test timeouts without another provider call, official init scaffolds require named dispatch roles, and Grok paid cells pause on short-lived credentials. | Run `al upgrade plan` then `al upgrade`. | The v0.18.3 migration manifest is empty; changes are additive dispatch evidence and benchmark runtime, recovery, and observability fixes and require no manual migration. It preserves the `0.10.2` migration floor. |
| `v0.18.2` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0`, `0.17.1`, `0.17.2`, `0.17.3`, `0.17.4`, `0.17.5`, `0.17.6`, `0.17.7`, `0.17.8`, `0.17.9`, `0.18.0`, `0.18.1` | `safe auto`: Paid benchmark provider output is checkpointed before verification and can be replayed through the verifier without another provider call. Benchmark phase reporting, graceful cancellation, Linux ownership repair, and artifact retention are hardened. | Run `al upgrade plan` then `al upgrade`. | The v0.18.2 migration manifest is empty; changes are benchmark runtime, recovery, observability, and cleanup fixes and require no manual migration. It preserves the `0.10.2` migration floor. |
| `v0.18.1` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0`, `0.17.1`, `0.17.2`, `0.17.3`, `0.17.4`, `0.17.5`, `0.17.6`, `0.17.7`, `0.17.8`, `0.17.9`, `0.18.0` | `safe auto`: Benchmark runtime preflights now persist content-addressed receipts and reuse them on later identical invocations, and warn when DeepSWE `linux/amd64` task containers require host emulation. Retained Grok and Antigravity streams are validated before paid provider calls. | Run `al upgrade plan` then `al upgrade`. | The v0.18.1 migration manifest is empty; changes are runtime, benchmark, or validation fixes and require no manual migration. It preserves the `0.10.2` migration floor. |
| `v0.18.0` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0`, `0.17.1`, `0.17.2`, `0.17.3`, `0.17.4`, `0.17.5`, `0.17.6`, `0.17.7`, `0.17.8`, `0.17.9` | `safe auto`: Adds `al update` for installation-aware global CLI updates, hardens Homebrew and script-install detection, and prevents duplicate built-in Agent Dispatch discovery or version-dispatch leakage into MCP children. | Run `al upgrade plan` then `al upgrade`. | The v0.18.0 migration manifest is empty; the release adds a global CLI command and runtime safeguards without requiring manual migration. It preserves the `0.10.2` migration floor. |
| `v0.17.9` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0`, `0.17.1`, `0.17.2`, `0.17.3`, `0.17.4`, `0.17.5`, `0.17.6`, `0.17.7`, `0.17.8` | `safe auto`: Benchmark study preparation now preflights runtime per task and experiment, retains task images across serial cells before reclaiming them, stabilizes overlay certification identity, and uses Grok's disposable-container sandbox without a host Bubblewrap dependency. | Run `al upgrade plan` then `al upgrade`. | The v0.17.9 migration manifest is empty; changes are runtime, benchmark, or validation fixes and require no manual migration. It preserves the `0.10.2` migration floor. |
| `v0.17.8` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0`, `0.17.1`, `0.17.2`, `0.17.3`, `0.17.4`, `0.17.5`, `0.17.6`, `0.17.7` | `safe auto`: Adds `al benchmark init` and a self-contained DeepSWE benchmark workflow with automatic concurrency, Docker capacity preflight, task-image reclamation, and progress reporting. Fixes Antigravity structured terminal responses being truncated at the metadata limit. | Run `al upgrade plan` then `al upgrade`. | The v0.17.8 migration manifest is empty; changes are additive, runtime, or validation-only and require no manual migration. It preserves the `0.10.2` migration floor. |
| `v0.17.7` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0`, `0.17.1`, `0.17.2`, `0.17.3`, `0.17.4`, `0.17.5`, `0.17.6` | `safe auto`: Agent Dispatch now uses structured provider output internally for every supported agent, including Antigravity display-name models, while preserving plain-text answers at the caller boundary. Fixes Antigravity exact-slug dispatches failing before provider launch. | Run `al upgrade plan` then `al upgrade`. | The v0.17.7 migration manifest is empty; changes are runtime fixes and require no manual migration. It preserves the `0.10.2` migration floor. |
| `v0.17.6` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0`, `0.17.1`, `0.17.2`, `0.17.3`, `0.17.4`, `0.17.5` | `safe auto`: Fixes empty MCP preflight contract encoding and the pinned `quill-shared-toolbar-focus` readiness image. Benchmark readiness adds bounded task selection, sharding, timeouts, and progress reporting. | Run `al upgrade plan` then `al upgrade`. | The v0.17.6 migration manifest is empty; changes are runtime, benchmark-certification, or release-process fixes and require no manual migration. It preserves the `0.10.2` migration floor. |
| `v0.17.5` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0`, `0.17.1`, `0.17.2`, `0.17.3`, `0.17.4` | `safe auto`: Fixes empty MCP preflight contracts encoding `servers` as `null` instead of an empty array when no MCP servers are enabled. | Run `al upgrade plan` then `al upgrade`. | The v0.17.5 migration manifest is empty; the change is a runtime contract fix and requires no manual migration. It preserves the `0.10.2` migration floor. |
| `v0.17.4` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0`, `0.17.1`, `0.17.2`, `0.17.3` | `safe auto`: Release preflight now certifies the pinned DeepSWE benchmark catalog before artifact builds, benchmark selections use exact canonical provider model identities, and `.agent-layer/.env` is optional when no secrets are needed. | Run `al upgrade plan` then `al upgrade`. | The v0.17.4 migration manifest is empty; changes are additive, validation-only, or handled at runtime. It preserves the `0.10.2` migration floor. |
| `v0.17.3` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0`, `0.17.1`, `0.17.2` | `safe auto`: Adds pinned Antigravity and Grok benchmark adapters for `al benchmark run`, provider-native Agent Layer treatments, structured coordinator/dispatch evidence, and usage-based API-equivalent cost accounting. Raises the tested Antigravity baseline to 1.1.21 for the structured headless output required by benchmark child usage. | Run `al upgrade plan` then `al upgrade`. | The v0.17.3 migration manifest is empty; changes are additive or handled at runtime. The release adds a template ownership manifest and preserves the `0.10.2` migration floor. |
| `v0.17.2` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0`, `0.17.1` | `safe auto`: Hardens hidden `al organize-scratch` so dry runs are read-only, apply records actual outcomes, and repository roots plus worktrees stay protected. Codex project trust now uses the physical repository path after symlink resolution. `make dev` is a formatting and lint loop; `make ci` remains the full verification gate. Public website copy is rewritten for clarity. | Run `al upgrade plan` then `al upgrade`. | The v0.17.2 migration manifest is empty; all changes are additive or handled at runtime. It preserves the `0.10.2` migration floor. |
| `v0.17.1` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3`, `0.17.0` | `safe auto`: The Claude instruction shim is now `.claude/CLAUDE.md` instead of root `CLAUDE.md`. When Grok is enabled, Claude compatibility is disabled in Grok configuration so Grok does not load duplicate instructions. Adds `al skills diff <name>` for live tree comparisons, conflict resolution workspaces under `.agent-layer/tmp/skill-conflicts/<name>/` via `al skills resolve <name>`, and `read-pr-comments.sh` in `ship-pr`. | Run `al upgrade plan` then `al upgrade`. | Migration manifest includes one `delete_generated_artifact` operation (`CLAUDE.md`) to remove the generated root Claude instruction shim; hand-authored root `CLAUDE.md` files are left untouched. Preserves the `0.10.2` migration floor. |
| `v0.17.0` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2`, `0.16.3` | `safe auto`: Adds Grok Build CLI as a supported agent client (`al grok`, `[agents.grok]` config section, native `.grok/config.toml` projection, shared `.agents/skills/` skills, and Agent Dispatch target `grok`). | Run `al upgrade plan` then `al upgrade`. If you want Grok support, set `[agents.grok] enabled = true` after upgrading or re-run `al wizard`. | Migration manifest includes one `config_set_default` operation (`agents.grok.enabled = false`). No breaking changes. |
| `v0.16.3` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1`, `0.16.2` | `safe auto`: Generated Agent Dispatch MCP launchers enter the repository root recorded by `al sync` before invoking `al`, so the global shim resolves that repository’s pin even when an MCP client starts the server elsewhere. When the MCP client started from a directory in that same Agent Layer project, the launcher retains that directory as the dispatch working directory. | Run `al upgrade plan` then `al upgrade`. Refresh existing generated MCP files once with `AL_VERSION=v0.16.3 al sync`; this refresh does not change the repository pin. | The v0.16.3 migration manifest is empty; the generated-MCP refresh is a projection update handled by `al sync`. It preserves the `0.10.2` migration floor, and the launcher retains the legacy `al dispatch mcp-server` arguments so cached older binaries continue to work. |
| `v0.16.2` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0`, `0.16.1` | `safe auto`: Adds `al benchmark correct-scores` for regenerating versioned canonical DeepSWE results from preserved verifier evidence; benchmark reports now fail rather than emit a known-incorrect score. The skill lock file gains schema version 2, which adds one optional per-skill publication checkpoint; this release reads version 1 and version 2, and writes version 2 only when it next changes a lock, so an unchanged version 1 lock is left alone. A project with no `[[skills.imports]]` has no lock file at all and is unaffected. Upgrades from versions before v0.16.0 still apply the v0.16.0 skill, instruction, and import migrations. `needs review`: The generated `.agent-layer/.gitignore` stops ignoring `skills-imported/` and `skills.lock.json`, a generated-file ownership boundary change, so a project that commits `.agent-layer/` also commits its imported skills and lockfile. `al skills push` records the last published tree separately from the source lock, so repeated pushes to a contribution branch add commits to the same pull-request branch. | Run `al upgrade plan` then `al upgrade`. If you commit `.agent-layer/`, run `git add .agent-layer/skills-imported .agent-layer/skills.lock.json` once after upgrading, and comment out the `/.agent-layer/` line in `.agent-layer/gitignore.block` if the root `.gitignore` still ignores the whole directory. If you do commit the lock, every consumer of that repository needs v0.16.2 or newer, because v0.16.1 and earlier reject a version 2 lock with `unsupported schema version 2 (this Agent Layer supports 1)`. Agent Layer does not promise that an older binary can read newer state; pin the repository version to keep consumers aligned. | The v0.16.2 migration manifest is empty; all changes are additive or handled at runtime. It preserves the `0.10.2` migration floor, and chained upgrades still load the v0.16.0 operations. `.agent-layer/.gitignore` is agent-owned and rewritten on every `al init`/`al upgrade`, so the ignore-rule change needs no migration operation. A version-only difference never triggers a lock rewrite: the import transaction compares the old and new lock as marshaled bytes at the current schema version before committing. `.agent-layer/skills-imported/.staging/` remains ignored. |
| `v0.16.1` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0`, `0.16.0` | `safe auto`: Fixes init and upgrades incorrectly blocking on unmatched content in the exclusively owned disposable `.agents/skills/` and `.claude/skills/` roots. Upgrades from versions before v0.16.0 still apply the v0.16.0 skill, instruction, and import migrations. | No manual cleanup is required; run `al upgrade plan` then `al upgrade`. Any independently installed content in the disposable client skill roots is replaced on the next sync; move durable skills into `.agent-layer/skills/` or manage them through `al skills` first. | The v0.16.1 migration manifest is empty. Chained upgrades still load the v0.16.0 `claim_skill_projection_roots` operation, which remains a compatibility no-op; init and upgrade do not inspect disposable client-root contents. |
| `v0.16.0` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0`, `0.15.0` | `safe auto`: Adds Git-backed Agent Skill imports (`al skills add`, `remove`, `status`, `pull`, `reset`, `push`), a `[[skills.imports]]` config block, and a bundled `skill-sync` catalog skill; renames the managed `02_memory.md` instruction file to `01_memory.md` and the `agent-dispatch` catalog skill to `dispatch-agent`. `needs review`: Agent Layer now owns `.agents/skills/` and `.claude/skills/` completely, replacing enabled roots wholesale and removing disabled ones; the workflow skill bundle is consolidated so `implement` replaces the `plan-work`/`review-plan`/`implement-plan`/`fully-implement-plan`/`full-workflow` chain and `ship-pr` absorbs `address-pr-comments` and `fix-ci`; `boost-coverage`, `clean-and-fix-code`, `debug-and-fix-issue`, `improve-codebase`, `review-uncommitted-code`, `run-and-fix-all-checks`, `schedule-backlog`, `simplify-codebase`, and `verify-work` are removed; the instruction set is consolidated from five files to two; `docs/agent-layer/ROADMAP.md` is retired as a managed memory file. | Run `al upgrade plan` then `al upgrade`. Review managed skill and instruction changes if you customized them. Before upgrading, back up or remove any hand-authored entry under `.agents/skills/` or `.claude/skills/` that Agent Layer did not generate: the migration blocks before mutation if such a root contains an entry that is neither marker-bearing output from a released Agent Layer version nor a directory matching a source-tier skill with a canonical regular `SKILL.md`. Update automation or references that invoke `/agent-dispatch` to `/dispatch-agent`. Removed workflow skills and the superseded `01_base.md`, `03_tools.md`, and `04_conventions.md` instruction files are reported as orphans and left on disk; move anything you want to keep into `00_rules.md`, then delete them by hand. | The migration manifest contains one `claim_skill_projection_roots` operation and source-agnostic `rename_file` operations for `.agent-layer/instructions/02_memory.md` -> `01_memory.md`, and `.agent-layer/skills/agent-dispatch` -> `.agent-layer/skills/dispatch-agent`; each rename fails loudly if a populated destination already exists. It preserves the `0.10.2` migration floor. `al upgrade` still understands the `memory_roadmap_v1` ownership policy recorded in manifests from earlier releases, so upgrading a repo that has a `ROADMAP.md` continues to work; the file is left untouched and becomes an ordinary project document. Imported skills are machine-managed under `.agent-layer/skills-imported/` with `skills.lock.json` state, both git-ignored. |
| `v0.15.0` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0`, `0.14.0` | `safe auto`: Adds the built-in `agent-layer` MCP server for Agent Dispatch, projecting `dispatch_options`, `dispatch_start`, `dispatch_wait`, `dispatch_continue`, and `dispatch_cancel` into enabled Codex, Claude, Antigravity, VS Code, and Copilot CLI clients; adds website-planned DeepSWE benchmark baseline, treatment, and report commands. `needs review`: Simplifies bundled workflow skills and changes the Agent Dispatch skill to use MCP tools; renames the bundled `playwright-cli` skill to `playwright`. | Run `al upgrade plan` then `al upgrade`. Review managed workflow skill and instruction changes if you customized them. If you maintain automation or custom references to `.agent-layer/skills/playwright-cli/`, update them to `.agent-layer/skills/playwright/` after the migration. | The migration manifest contains one source-agnostic `rename_file` operation for `.agent-layer/skills/playwright-cli` -> `.agent-layer/skills/playwright` and preserves the `0.10.2` migration floor. Agent Dispatch MCP and CLI use the same asynchronous handles, terminal states, and durable result files; Antigravity runtime MCP discovery remains dependent on the installed provider capability. |
| `v0.14.0` | `0.10.2`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3`, `0.13.0` | `breaking/manual`: Agent Dispatch now exposes asynchronous handle-based lifecycle commands: `start`, `wait`, `continue`, and `cancel`. The prior synchronous fresh/resume syntax and public `fanout`, `inspect`, `history`, `list`, and `delete` operations are removed. `safe auto`: retired per-agent dispatch default tables are removed before strict config decoding. | Update dispatch scripts and workflows to call `al dispatch start --agent <agent> --prompt <text>`, retain the returned `handle`, then call `al dispatch wait <handle>` until it returns the terminal state and result path. Use `al dispatch continue <handle> --prompt <text>` only after the current invocation is terminal. Replace fanout with independent starts. | The migration manifest deletes `agents.antigravity.dispatch`, `agents.claude.dispatch`, and `agents.codex.dispatch`, including their `default_agent` values. It retains the established `0.10.2` migration floor so source-agnostic cleanup also runs for supported legacy upgrades. Every successful lifecycle command writes one JSON object; completed answers are immutable Markdown files named by `result_path`. |
| `v0.13.0` | `0.10.x`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2`, `0.12.3` | `breaking/manual`: Agent Dispatch is final-answer-only and fresh by default. The v1 streaming and `dispatch_capable` options contract is removed; explicit `resume`, `fanout`, `inspect`, `history`, `cancel`, `list`, and `delete` replace implicit lifecycle assumptions. `needs review`: workflow root skills are renamed/retired, `fix-issues` is absorbed by the mode-driven `auto-skill-loop`, and the version-binary handoff package is unambiguously named `internal/versiondispatch`. | Update scripts and automations that parse dispatch progress, provider stderr, raw events, or v1 `dispatch_capable` fields. Consume only final stdout, save the emitted friendly name when later continuation is intended, and use `al dispatch resume <name> ...` rather than inferring a conversation from a target or prompt. Back up any customized retired or renamed skill directory, then run `al upgrade plan` and `al upgrade`; review managed workflow skill changes and approve removal only for retired directories you no longer need. | Migration manifest renames whole tracked skill directories and carries bundled resources through those renames. Retired directories, including `fix-issues`, use the ownership-aware reviewed removal path rather than an unconditional directory deletion. The release also adds the 0.13.0 ownership baseline. Claude Code 2.1.207, Codex CLI 0.144.1, and Antigravity 1.1.1 are the tested provider baselines; newer versions run with a compatibility warning, while older, unreadable, and malformed versions fail before launch. Existing provider conversations are not deleted or imported. |
| `v0.12.3` | `0.10.x`, `0.11.0`, `0.12.0`, `0.12.1`, `0.12.2` | `safe auto`: Adds current Codex model and reasoning-effort suggestions, and fixes `al sync` reconciliation of generated skill resources. Sync now removes stale generated resources, handles file/directory transitions, and replaces destination symlinks safely without following source symlinks. | No manual action is required. Run `al upgrade plan` then `al upgrade`; the subsequent sync reconciles generated skill resources. | Migration manifest has no operations. Existing configurations accept the new suggested values without modification because Codex model and reasoning-effort fields already allow custom values. |
| `v0.12.2` | `0.10.x`, `0.11.0`, `0.12.0`, `0.12.1` | `needs review`: Patch release that tightens bundled skill and instruction contracts, adds Agent Layer-specific skill-architecture documentation, fixes installer prompt-routing edge cases, updates e2e assertions for the retired Codex instruction shim, and runs CI before importing release-signing credentials. | Run `al upgrade plan` then `al upgrade`. Review managed instruction and skill template updates if you customized the workflow bundle. No manual config or filesystem migration is required. | Migration manifest has no operations. Template ownership manifest changes identify the updated managed skill, instruction, and docs templates for deterministic upgrade planning. |
| `v0.12.1` | `0.10.x`, `0.11.0`, `0.12.0` | `needs review`: Patch release that fixes binary Homebrew formula publishing, preserves Codex chime hook trust state during sync, retires the generated `.codex/AGENTS.md` shim, and refreshes workflow instruction/skill templates. | Run `al upgrade plan` then `al upgrade`. Review managed instruction and skill template updates if you customized the workflow bundle. No manual Codex action is required for generated `.codex/AGENTS.md`; the migration deletes only the Agent Layer-generated shim, and hand-authored files at that path are preserved. | Migration manifest deletes generated `.codex/AGENTS.md` because Codex reads root `AGENTS.md` as project instructions and repo-local `CODEX_HOME` can cause the shim to be loaded as duplicate home-level instructions. It preserves the `0.10.2` supported floor so unpinned legacy upgrades still discover source-agnostic migrations through the chained planner. |
| `v0.12.0` | `0.10.x`, `0.11.0` | `needs review`: Adds typed Antigravity model selection, configurable Agent Dispatch depth, repo-local Codex home opt-in, shared Codex TOML patching, provider turn-stop chimes, serialized sync writes, signed/notarized macOS binaries, binary Homebrew delivery, and new workflow skills. | Run `al upgrade plan` then `al upgrade`. The upgrade prompts for `agents.codex.local_config_dir`; choose `true` to keep repo-local Codex auth/session/log behavior under `.codex/`, or `false` to use Codex's normal global/project config layering. The upgrade also prompts for `notifications.chime`; choose `true` to enable provider turn-stop chimes, or `false` to keep them disabled. Re-run `al wizard` if you want to set an Antigravity model or change Agent Layer folder tracking. | Migration manifest moves `agents.antigravity.agent_specific.model` to `agents.antigravity.model`, sets `agents.codex.local_config_dir = false` by default for non-interactive upgrades, and sets `notifications.chime = false` by default for non-interactive upgrades. It preserves the `0.10.2` supported floor so unpinned legacy upgrades still discover Gemini-to-Antigravity source-agnostic migrations and v0.11.0 statusline defaults through the chained planner. When Codex local config is false or absent, Agent Layer preserves any inherited `CODEX_HOME` instead of setting or clearing it. |
| `v0.11.0` | `0.10.x` | `needs review`: Adds explicit opt-in provider statuslines for Claude Code and Codex. When enabled, Claude sync projects editable `.agent-layer/claude-statusline.sh` to `.claude/claude-statusline.sh` and wires `statusLine` in `.claude/settings.json`; Codex sync merges editable `.agent-layer/codex-statusline.toml` into `.codex/config.toml` unless `agents.codex.agent_specific.tui.status_line` is set. | Run `al upgrade plan` then `al upgrade`. The upgrade prompts for `agents.claude.statusline` and `agents.codex.statusline`; choose `true` to enable and seed any missing source file, or choose `false` to keep disabled. Claude's default statusline script requires `jq` on `PATH` at runtime. | Absent statusline keys mean disabled after v0.11.0. Migration manifest prompts write explicit true/false values. Missing enabled sources are seeded once; existing source files are user-owned, skipped by non-interactive upgrade, and replaced only after an interactive diff review. For unpinned legacy upgrades, the manifest also carries forward v0.10.2 Gemini->Antigravity source-agnostic operations until the known unpinned migration-chain issue is resolved. |
| `v0.10.2` | `0.10.x` | `safe auto`: Replaces Gemini CLI support and the retired Antigravity desktop launcher with Antigravity (`al agy`, `[agents.antigravity]`, repo-local `.agy/` config, and `agy --gemini_dir` containment). Adds `al probe agy` for observed permissions/MCP capability reporting. Removes `al gemini` entirely (no deprecation window); existing scripts must switch to `al agy`. Adds `.antigravitycli/` and `.agy/` to the gitignore template, keeps `/.gemini/` ignored for stale generated config safety, and stops generating `GEMINI.md`. Fresh installs default `[agents.antigravity] enabled = false`. Removes the Opus-only requirement for Claude `reasoning_effort`: a value set with a non-Opus or unset `agents.claude.model` now passes validation (Claude Code applies the effort where the active model supports it), and `al wizard` offers the reasoning-effort prompt for any enabled Claude model. | Run `al upgrade plan` then `al upgrade`. Existing `[agents.gemini] enabled = true/false` is migrated to `[agents.antigravity] enabled = true/false`; MCP server client lists are migrated from `gemini` to `antigravity`; stale Gemini `model` and `reasoning_effort` keys plus the retired Antigravity desktop table are removed (any pre-existing `[agents.antigravity]` enable flag from the retired desktop launcher is replaced by the migrated Gemini value or the default when legacy Gemini is present; current Antigravity enablement is preserved when no legacy Gemini table exists). Any stale `GEMINI.md` shim in the repo root is deleted (only when it carries the Agent Layer `GENERATED FILE` watermark; hand-authored files at that path survive). The upgrade snapshot captures the original. Run `al probe agy` after upgrade if you rely on Antigravity MCP behavior. | Migration manifest includes `config_delete_key` (`agents.antigravity`, gated for unknown-source upgrades unless legacy Gemini is present), `config_rename_key` (`agents.gemini.enabled` -> `agents.antigravity.enabled`), `config_replace_string` (`mcp.servers[].clients[]`: `gemini` -> `antigravity`), `config_delete_key` (`agents.gemini`), `config_set_default` (`agents.antigravity.enabled = false`), and `delete_generated_artifact` (`GEMINI.md`). As of v0.10.2 the agy v1.0.0 probe baseline writes and migrates Antigravity MCP config, but runtime MCP registration is not yet observed; re-check with `al probe agy` when upgrading agy. |
| `v0.10.1` | `0.10.x` | `safe auto`: Adds two new diff-scoped cleanup skills (`prune-new-tests`, `simplify-new-code`); renames `simplify-code` skill to `simplify-codebase` (full-repo scope) and removes the legacy directory; wires fresh-context reviewer subagents into `implement-plan`, `audit-and-fix-uncommitted-changes`, `verify-against-plan`, `address-pr-comments`, and `improve-codebase`; adds `al init --here` flag for in-place installs in subdirectories of existing repos; deep-merges Claude `agent_specific` configuration so `permissions.deny` is additive (arrays/scalars still replace); auto-writes a Codex per-repo `[projects."<repo root>"] trust_level = "trusted"` stanza to repo-local `.codex/config.toml`; ships `agent_specific.permissions.deny = ["AskUserQuestion"]` in the install seed so fresh `al init` disables Claude Code's clarification-question tool by default (existing repos unaffected); adds a human-gated merge phase to `ship-pr`. | Run `al upgrade plan` then `al upgrade`. The upgrade removes the legacy `.agent-layer/skills/simplify-code/` directory; if you customized that skill, back it up first. Existing repos do not inherit the new install-seed `permissions.deny = ["AskUserQuestion"]`; add it manually to `agents.claude.agent_specific.permissions.deny` if you want the new default. The Codex per-repo trust stanza is written to `.codex/config.toml` on next sync. | Migration manifest includes one `delete_file` operation (`.agent-layer/skills/simplify-code`). No config schema changes. Claude `agent_specific` object values are now deep-merged into `.claude/settings.json`; `permissions.deny` is additive and does not trigger an override warning. |
| `v0.10.0` | `0.9.x` | `safe auto`: Skills consolidated into a shared `.agents/skills/` projection for non-Claude clients (Codex, Gemini, Antigravity, VS Code/Copilot, Copilot CLI); per-client directories (`.codex/skills/`, `.gemini/skills/`, `.agent/skills/`, `.vscode/prompts/`, `.github/skills/`) retired and cleaned by sync. `chat.agentSkillsLocations` written to `.vscode/settings.json`. Gemini sync migrated from deprecated `tools.allowed` to the Policy Engine; `.gemini/policies/agent-layer.toml` generated with `allowRedirection = true`. `al upgrade` now auto-runs `al sync` on success. `al upgrade` overwrite prompts show compact `+N -M` line-stat summary with opt-in full diff view. `.agent-layer/tmp/` excluded from snapshots; rollback and bulk-delete scoped accordingly. `xhigh` added as a valid `reasoning_effort` for Claude; custom values pass through with a warning. `_generatedBy: agent-layer` provenance field added to `.mcp.json` and `.gemini/settings.json`. MCP catalog decoupled from `config.toml` install seed into an embedded `mcp-catalog.toml`. | No manual action required; run `al upgrade plan` then `al upgrade`. The automatic post-upgrade sync will clean retired per-client skill directories and write `.gemini/policies/agent-layer.toml`. If you have customized `.codex/skills/`, `.gemini/skills/`, or `.vscode/prompts/`, back up any custom content before upgrading — sync will remove Agent Layer-managed files from those paths. | Migration manifest has no operations. All structural changes are applied by `al sync` during the automatic post-upgrade sync step. No config schema changes. |
| `v0.9.2` | `0.9.x` | `safe auto`: GitHub Copilot CLI added as a supported agent client (`al copilot`, `[agents.copilot_cli]` config section). Context file (`.context.md`) added to plan/task artifact system for faster implementing-agent orientation. `max` reasoning effort supported for Claude Opus models. `--effort` flag now defers to `agent_specific.effortLevel` when set. Model catalogs updated (added `gemini-3.1-flash-lite`, `opus[1m]`, `gpt-5.4`; removed deprecated models). Dispatch System interface consolidation replaces package-level mutable stubs with proper dependency injection. Memory hygiene and instruction quality improvements across templates. Skills improved: `audit-documentation` and `audit-tests` fix autonomously, all skills get tradeoff checkpoints, `ship-pr` edge cases fixed. Stale Copilot artifacts cleaned when agent is disabled. Explicit upgrade success message added. | No manual action required; run `al upgrade plan` then `al upgrade`. If you want GitHub Copilot CLI support, add `[agents.copilot_cli]` with `enabled = true` to `config.toml` after upgrading or re-run `al wizard`. | Migration manifest includes one `config_set_default` operation (`agents.copilot_cli.enabled = false`). No breaking changes. |
| `v0.9.1` | `0.9.x` | `safe auto`: Instruction files reordered for primacy effect (`02_rules.md` → `00_rules.md`, `00_base.md` → `01_base.md`, `01_memory.md` → `02_memory.md`); cross-file duplicates removed and verbose sections compressed. New user-managed `04_conventions.md` seeded on init and updated via `append_to_file` migrations (UTC-only internals and No system Python moved from rules to conventions). `needs review`: 5 legacy built-in skills replaced by normalized workflow equivalents (`cleanup-code` → `simplify-code`, `continue-roadmap` → `complete-current-phase`, `find-issues` → `resolve-findings`, `fix-tests` → `repair-checks`, `update-roadmap` → `schedule-backlog`); 17 new skills added; 5 existing skills restructured. Migration manifest deletes 6 deprecated skill directories. New site docs for skills and skill design. | Run `al upgrade plan` then `al upgrade`. The upgrade renames instruction files automatically. If you have custom edits in `00_base.md`, `01_memory.md`, or `02_rules.md`, verify they transferred correctly after the rename. Review `04_conventions.md` after upgrade and remove any conventions that do not apply to your project. If you renamed or customized the deleted skills (`cleanup-code`, `continue-roadmap`, `find-issues`, `fix-tests`, `update-roadmap`, `mechanical-cleanup`), back them up before upgrading. | Migration manifest includes 3 `rename_file`, 2 `append_to_file`, and 6 `delete_file` operations. `04_conventions.md` is user-owned: seeded once, never overwritten, and excluded from upgrade plan diffs. New `append_to_file` migration kind supports duplicate-detection and atomic writes. |
| `v0.9.0` | `0.8.x`, `0.7.x` (via migration chaining) | `safe auto`: Migration manifest support for `v0.9.0` includes canonical config-key migration (`agents.claude-vscode.enabled` -> `agents.claude_vscode.enabled`), required-field defaulting, legacy `.agent-layer/slash-commands/` -> `.agent-layer/skills/` rename, and automatic migration of all skills (built-in and user-authored) from flat format (`<name>.md`) to directory format (`<name>/SKILL.md`) with pre-flight conflict detection and user confirmation. Breaking-change metadata (`breaking_notice`, `breaking_details`) is now data-driven from the migration manifest. Upgrade output highlights readiness warnings and review-needed items in yellow for scanability. Unknown-file scanning now covers `docs/agent-layer/` in addition to `.agent-layer/`. Snapshot rollback accepts `created`-status snapshots for interrupted-upgrade recovery. New `CONTEXT.md` memory file template. `needs review`: [agentskills.io specification](https://agentskills.io/specification) alignment updates validation/normalization behavior (YAML frontmatter parsing, Unicode NFKC-aware name matching, rune-based limits, canonical `SKILL.md` naming). **Breaking:** Flat-format skills are no longer supported; all skills must use directory format after upgrading. | Run `al upgrade plan` then `al upgrade`. The upgrade will prompt to migrate any flat-format skills to directory format. If you have custom automation/scripts that still reference `.agent-layer/slash-commands/`, update them to `.agent-layer/skills/`. Use canonical `SKILL.md` naming (lowercase `skill.md` remains a compatibility fallback but triggers a warning). If you have custom files under `docs/agent-layer/` that are not memory file templates, the upgrade may prompt about them as unknown files. | Migration manifest includes one `config_rename_key`, one `config_set_default`, one directory rename, and one `migrate_skills_format` operation. Migration manifests now support `breaking`, `breaking_notice`, and `breaking_details` fields for data-driven breaking-change display. Flat-format skills (`<name>.md`) are no longer loaded; strict validation now catches malformed names/metadata earlier. |
| `v0.8.8` | `0.8.x`, `0.7.x` (via migration chaining) | `safe auto`: Canonicalized Claude VS Code config key naming to snake_case (`agents.claude_vscode.enabled`), strengthened release/docs contract checks, and hardened stable-release docs publishing/version retention behavior. | No manual action required; run `al upgrade plan` then `al upgrade`. If any custom automation or docs still reference `agents.claude-vscode.enabled`, update them to `agents.claude_vscode.enabled`. | Migration manifest includes `config_rename_key` (`agents.claude-vscode.enabled` -> `agents.claude_vscode.enabled`) and `config_set_default` (`agents.claude_vscode.enabled = false`) to keep required-field invariants intact. |
| `v0.8.7` | `0.8.x`, `0.7.x` (via migration chaining) | `safe auto`: Added quiet-mode support across runtime surfaces (`warnings.noise_mode = "quiet"` and one-off `--quiet`/`-q` client flags), plus wizard/config hardening and diff UX improvements: Escape back-navigation with first-step exit confirmation, explicit wizard section ordering policy, profile warning when replacing TOML-corrupt config, Claude reasoning-effort support (`low|medium|high`) with fail-loud model validation, explicit Gemini reasoning-effort unsupported-path errors, multiline TOML patch maintainability refactor, and colorized interactive upgrade diffs (plain-text fallback preserved). | No manual action required; run `al upgrade plan` then `al upgrade`. Optionally set `warnings.noise_mode = "quiet"` or use `al <client> --quiet` for one-off runs. Optionally set `[agents.claude] reasoning_effort = "low|medium|high"` when using supported Claude models. | No migration operations required. Existing configs remain valid. `noise_mode` now accepts `quiet`; new required-field migration guardrail test enforces future `config_set_default` parity for required fields introduced after baseline `0.8.1`. |
| `v0.8.6` | `0.8.x`, `0.7.x` (via migration chaining) | `safe auto`: Fixed `.gitignore` template where inline comments on `/.claude/` and `/.claude-config/` patterns caused Git to treat them as literal pattern text, leaving both directories unignored. `al wizard` now prompts for opt-in repo-local Claude settings and caches isolation (`local_config_dir`). Documentation now clarifies that Claude auth remains globally shared due to an upstream credential-store limitation. | No manual action required; run `al upgrade plan` then `al upgrade`. Users who installed v0.8.5 in a new repo should verify `.claude/` is gitignored after upgrading. | No config schema changes. Template hash will change due to the gitignore fix. |
| `v0.8.5` | `0.8.x`, `0.7.x` (via migration chaining) | `safe auto`: Consolidated release that includes strict unknown-key config validation, improved `.env` parsing, deterministic MCP tool-collision warnings, Gemini trusted-folder auto-write behavior, snapshot validation for zero-byte files, `al upgrade rollback --list`, agent-specific Claude/Codex passthrough config (`agents.<agent>.agent_specific`), opt-in repo-local Claude config isolation (`agents.claude.local_config_dir`), and hardened prompt-server dispatch/root validation for `al mcp-prompts`. | No manual action required; run `al upgrade plan` then `al upgrade`. Optionally enable new behavior by setting `local_config_dir = true` and/or adding `agent_specific` keys under Claude/Codex agent sections. | No migration operations are required. If Gemini trust-file writes fail, sync still completes and emits a warning with manual trust guidance. Config schema is unchanged. |
| `v0.8.4` | `0.8.x`, `0.7.x` (via migration chaining) | `safe auto`: Config validation now silently strips transport-incompatible MCP fields instead of erroring. Version dispatch diagnostic no longer prints twice during version-pinned dispatch. Wizard config patching handles dotted sub-key removal. New scenario-based e2e test framework (internal). | No manual action required; run `al upgrade plan` then `al upgrade`. | No config schema changes. Configs that previously failed validation due to leftover transport-incompatible fields (e.g., `headers` on a stdio server) will now load successfully. |
| `v0.8.3` | `0.8.x`, `0.7.x` (via migration chaining) | `safe auto`: Bug fixes only. `al doctor` no longer produces false "missing environment variables" warnings for MCP servers that reference built-in variables like `${AL_REPO_ROOT}`. `al wizard` now removes transport-incompatible fields (e.g., `headers` on stdio servers) during config patching, fixing a circular error where the wizard completed but sync failed. | No manual action required; run `al upgrade plan` then `al upgrade`. | No config schema changes. |
| `v0.8.2` | `0.8.x`, `0.7.x` (via migration chaining) | `safe auto`: Migration manifests are now chained during multi-version upgrades. Users jumping from 0.8.0 directly to 0.8.2 will now receive the `agents.claude-vscode.enabled` config prompt that was introduced in v0.8.1. Config resilience improvements ensure `al wizard`, `al doctor`, and `al upgrade` always work even on broken configs. Removed slash command `auto-approve` frontmatter; approval permissions are controlled entirely by `approvals.mode`. Slash command frontmatter now rejects unrecognized keys. | No manual action required; run `al upgrade plan` then `al upgrade`. If any custom slash commands contain `auto-approve` or other non-`description` frontmatter keys, remove them before upgrading. | Migration chaining loads all intermediate manifests between source and target versions. When source version is unknown, only the target manifest is loaded (backward compatible). |
| `v0.8.1` | `0.8.x` | `safe auto`: Added `[agents.claude-vscode]` config section for Claude Code VS Code Extension support. Added `approvals.mode = "yolo"` for maximum agent autonomy in sandboxed environments. `al vscode` now launches when either `[agents.vscode]` or `[agents.claude-vscode]` is enabled. | No manual action required; run `al upgrade plan` then `al upgrade`. If you use `[agents.vscode]` only (without `[agents.claude-vscode]`), behavior is unchanged. If you want Claude Code VS Code Extension support, add `[agents.claude-vscode]` with `enabled = true` to `config.toml`. | `CODEX_HOME` is now cleared from the VS Code process environment when `[agents.vscode]` is disabled, preventing stale Codex config leakage. (Superseded in v0.11.2, which preserves inherited `CODEX_HOME` instead of clearing it.) |
| `v0.8.0` | `0.7.x` | `breaking/manual`: `al init` is now one-time scaffolding and no longer supports upgrade/repair; `al init --overwrite` and `al init --force` removed. `needs review`: `al upgrade` no longer supports a single destructive `--force` mode and now requires explicit apply categories. `safe auto`: `al upgrade` now creates automatic managed-file snapshots, auto-rolls back on upgrade-step failures, and supports manual restore via `al upgrade rollback <snapshot-id>`. | Replace any automation that runs `al init --force` or `al upgrade --force` with explicit apply flags (for example `al upgrade --yes --apply-managed-updates`). Include `--apply-memory-updates` and/or `--apply-deletions` only when intentionally applying those categories. For interactive flows, run `al upgrade plan` then `al upgrade`. Use `al upgrade rollback <snapshot-id>` only when you need to restore a previously applied snapshot. | `al upgrade` never overwrites `.agent-layer/.env` or `.agent-layer/config.toml`. Root `.gitignore` managed block is still updated automatically and is excluded from upgrade plans. Snapshots are stored under `.agent-layer/state/upgrade-snapshots/`. |
| `v0.7.0` | `0.6.x` | `breaking/manual`: Windows support removed (installer, launcher, release targets, code paths). `safe auto`: upgrade path hardening (pin recovery, init dispatch bypass, download progress/errors), upgrade contract published. | **Windows users:** Windows is no longer supported; remove `al-install.ps1` and `open-vscode.bat` if present and migrate to macOS or Linux. **All other users:** No manual action required; run `al upgrade plan` and `al upgrade`. | First release with published upgrade contract. |

Migration execution rules for supported release-line upgrades:
- Each supported target release ships an embedded migration manifest at `internal/templates/migrations/<target>.json`, including `min_prior_version`.
- `al upgrade` executes migration operations before template writes and emits a deterministic migration report.
- If source version resolution fails, source-agnostic operations still run; source-gated operations are skipped and reported.
- `.agent-layer/.env` is namespace-scoped: only keys prefixed with `AL_` are loaded. Non-`AL_` keys are ignored and there is no env-key migration path.

How this table is maintained:
- Add or update a row for every release.
- If a release includes `breaking/manual` items, list exact required actions.
- If no special migration is needed, keep the row explicit to avoid ambiguity.

## Documentation version retention and access

Agent Layer website docs are versioned per release, then retained with a bounded policy to keep the published set stable and small:

1. Keep the newest 4 patch releases from the newest minor line.
2. Keep the newest patch release for each of the newest 4 minor lines (including the newest minor line).
3. Keep stable releases only (prereleases are dropped).
4. Keep the union of both sets in newest-first order.

How to access older docs:

1. Use the docs version selector in the website navigation to switch between retained versions.
2. If a version is not listed there, it has been pruned from the hosted site by retention policy.
3. For pruned versions:
   - Use the [Changelog](/changelog) for release-level historical behavior.
   - Browse source docs at the matching tag in the main repo:
     - `https://github.com/conn-castle/agent-layer/tree/vX.Y.Z/site/docs`

### Ownership baseline and manifest policy

Upgrade ownership classification is deterministic and offline:

- Release manifests are committed in-repo under `internal/templates/manifests/<version>.json`.
- Backfill manifests are committed for `v0.6.0`, `v0.6.1`, and `v0.7.0`.
- New releases must add a manifest for the new tag as part of release prep.
- Runtime upgrade planning does not call GitHub/tag APIs to infer ownership.

Repo-local baseline state is stored at:

- `.agent-layer/state/managed-baseline.json`

`al upgrade plan` compares local files against baseline evidence plus embedded manifests and emits `unknown_no_baseline` when ownership cannot be proven safely.

## OS and shell capability matrix

Windows is not supported.

| Surface | macOS | Linux | Notes |
| --- | --- | --- | --- |
| Install via Homebrew | Supported | Supported | Requires Homebrew setup for platform. |
| Install via `al-install.sh` | Supported | Supported | Requires `curl` and `sha256sum` or `shasum`. |
| `al` CLI core commands | Supported | Supported | Includes `init`, `upgrade`, `sync`, `doctor`, client launch commands. |
| VS Code launchers | Supported | Supported | macOS: `.app`/`.command`; Linux: `.desktop`/`.sh`. |
| Shell completion: `bash` | Supported | Supported | `al completion bash --install`. |
| Shell completion: `zsh` | Supported | Supported | `al completion zsh --install`; may require `fpath` setup. |
| Shell completion: `fish` | Supported | Supported | `al completion fish --install`. |

## Commitment level and limitations

These guarantees are explicit without over-promising:

- We commit to best-effort adherence to this model and guarantee scope.
- If we cannot fully meet a guarantee in a specific release, we will document the limitation clearly in release notes and migration guidance.
- When in doubt, prefer explicit communication over silent assumptions.
